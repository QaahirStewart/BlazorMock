@page "/pokemon-examples/step4"
@inject ILearningProgressService ProgressService
@inject NavigationManager Navigation
@inject IJSRuntime JS
@implements IDisposable
@rendermode InteractiveServer

<PageTitle>Pokemon Step 4: Loading & Error States</PageTitle>

<div class="min-h-screen py-8">
    <div class="w-full mx-auto px-4 max-w-5xl">
        <a href="/pokemon-guide" class="inline-flex items-center gap-2 text-blue-600 hover:text-blue-700 mb-4">
            <span>←</span> Back to Pokemon Guide
        </a>

        <div class="rounded-2xl overflow-hidden border-2 border-gray-200 bg-white mb-6">
            <div class="h-2 w-full bg-gradient-to-r from-blue-200 via-blue-300 to-emerald-200"></div>
            <div class="p-5 sm:p-6">
                <h1 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-2">
                    ⏳ Step 4: Loading & Error States
                </h1>
                <p class="text-gray-600">
                    Handle loading, error, and empty states to provide better user experience.
                </p>
            </div>
        </div>

        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">What You'll Learn</h2>
            <ul class="list-disc ml-6 text-sm sm:text-base text-gray-700 space-y-2">
                <li>Add loading spinner while fetching data</li>
                <li>Display error messages when API calls fail</li>
                <li>Handle empty state when no results found</li>
                <li>Use try-catch for robust error handling</li>
            </ul>
        </div>

        <div class="bg-white rounded-2xl p-5 sm:p-6 mb-6 border-2 border-gray-200">
            <h2 class="text-xl font-bold text-gray-900 mb-4">📝 Code Example</h2>
            
            <div class="mb-4">
                <h3 class="text-sm font-semibold text-gray-700 mb-2">1. Add state flags</h3>
                <pre class="bg-gray-100 p-4 rounded-lg text-xs sm:text-sm font-mono overflow-x-auto">@@code {
    private bool isLoading = true;
    private string? errorMessage;
    private List&lt;PokemonItem&gt; allPokemon = new();
}</pre>
            </div>

            <div class="mb-4">
                <h3 class="text-sm font-semibold text-gray-700 mb-2">2. Wrap API calls in try-catch</h3>
                <pre class="bg-gray-100 p-4 rounded-lg text-xs sm:text-sm font-mono overflow-x-auto">protected override async Task OnInitializedAsync()
{
    try
    {
        isLoading = true;
        errorMessage = null;
        
        var response = await Http.GetFromJsonAsync&lt;PokemonListResponse&gt;(
            "https://pokeapi.co/api/v2/pokemon?limit=151"
        );
        
        allPokemon = response?.Results ?? new();
    }
    catch (Exception ex)
    {
        errorMessage = $"Failed to load Pokemon: {ex.Message}";
    }
    finally
    {
        isLoading = false;
    }
}</pre>
            </div>

            <div class="mb-4">
                <h3 class="text-sm font-semibold text-gray-700 mb-2">3. Render conditional UI</h3>
                <pre class="bg-gray-100 p-4 rounded-lg text-xs sm:text-sm font-mono overflow-x-auto">@@if (isLoading)
{
    &lt;div class="flex items-center justify-center p-12"&gt;
        &lt;div class="animate-spin rounded-full h-12 w-12 border-4 
                    border-blue-600 border-t-transparent"&gt;&lt;/div&gt;
        &lt;span class="ml-3 text-gray-600"&gt;Loading Pokemon...&lt;/span&gt;
    &lt;/div&gt;
}
else if (!string.IsNullOrEmpty(errorMessage))
{
    &lt;div class="bg-red-50 border-2 border-red-200 rounded-xl p-6"&gt;
        &lt;h3 class="text-red-800 font-semibold mb-2"&gt;⚠️ Error&lt;/h3&gt;
        &lt;p class="text-red-700"&gt;@@errorMessage&lt;/p&gt;
        &lt;button @@onclick="RetryLoad" 
                class="mt-4 px-4 py-2 bg-red-600 text-white rounded"&gt;
            Retry
        &lt;/button&gt;
    &lt;/div&gt;
}
else if (allPokemon.Count == 0)
{
    &lt;div class="text-center p-12 text-gray-500"&gt;
        &lt;p class="text-lg"&gt;No Pokemon found&lt;/p&gt;
    &lt;/div&gt;
}
else
{
    @@* Render pokemon grid *@@
}</pre>
            </div>

            <div class="mb-4">
                <h3 class="text-sm font-semibold text-gray-700 mb-2">4. Add retry method</h3>
                <pre class="bg-gray-100 p-4 rounded-lg text-xs sm:text-sm font-mono overflow-x-auto">private async Task RetryLoad()
{
    await OnInitializedAsync();
    StateHasChanged();
}</pre>
            </div>
        </div>

        <div class="bg-yellow-50 border-2 border-yellow-200 rounded-2xl p-5 sm:p-6 mb-6">
            <h3 class="text-lg font-bold text-yellow-900 mb-2">💡 Pro Tips</h3>
            <ul class="list-disc ml-6 text-sm text-yellow-900 space-y-1">
                <li>Always use <code class="bg-white px-1 rounded">finally</code> to ensure <code class="bg-white px-1 rounded">isLoading</code> gets set to false</li>
                <li>Consider using a skeleton loader instead of a simple spinner</li>
                <li>Log errors to console for debugging: <code class="bg-white px-1 rounded">Console.WriteLine(ex)</code></li>
                <li>Show user-friendly error messages, not raw exception text</li>
            </ul>
        </div>

        <div class="bg-white rounded-xl p-6 border-2 border-green-200 mb-6">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div>
                    <h3 class="text-lg font-semibold text-gray-900 mb-1">Finished Step 4?</h3>
                    <p class="text-sm text-gray-600">Mark complete when loading/error states work</p>
                </div>
                @if (isComplete)
                {
                    <div class="flex items-center gap-3">
                        <span class="px-4 py-2 rounded-full bg-green-100 text-green-700 font-medium">✓ Completed</span>
                        <button @onclick="ResetStep"
                            class="px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors text-sm">Reset</button>
                    </div>
                }
                else
                {
                    <button @onclick="MarkComplete"
                        class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg font-medium transition-colors">Mark as Complete</button>
                }
            </div>
        </div>

        <div class="flex items-center justify-between">
            <a href="/pokemon-examples/step3"
                class="inline-flex items-center gap-2 px-4 py-2 border-2 border-gray-200 rounded-lg hover:bg-gray-50 transition-colors">
                <span>←</span>
                Previous: Step 3
            </a>
            <a href="/pokemon-examples/step5"
                class="inline-flex items-center gap-2 px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition-colors">
                Next: Step 5
                <span>→</span>
            </a>
        </div>
    </div>
</div>

@code {
    private bool isComplete;
    private IJSObjectReference? _copyModule;

    protected override async Task OnInitializedAsync()
    {
        var step = await ProgressService.GetStepAsync("pokemon", 4);
        isComplete = step?.IsComplete ?? false;
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            try
            {
                _copyModule = await JS.InvokeAsync<IJSObjectReference>("import", "./js/codeblocks.js");
                await _copyModule.InvokeVoidAsync("enhancePreBlocks");
            }
            catch (Exception ex)
            {
                Console.WriteLine($"Failed to load code block enhancer: {ex.Message}");
            }
        }
    }

    private async Task MarkComplete()
    {
        await ProgressService.MarkStepCompleteAsync("pokemon", 4);
        isComplete = true;
    }

    private async Task ResetStep()
    {
        await ProgressService.MarkStepIncompleteAsync("pokemon", 4);
        isComplete = false;
    }

    public void Dispose()
    {
        _copyModule?.DisposeAsync();
    }
}
