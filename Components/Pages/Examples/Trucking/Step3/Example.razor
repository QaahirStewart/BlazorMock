@page "/trucking-examples/step3"
@rendermode InteractiveServer
@inherits ExampleBase

<PageTitle>Trucking Step 3: Reusable Components - Example</PageTitle>

<div class="min-h-screen py-8 rounded-4xl bg-gray-50">
    <div class="w-full mx-auto px-4 max-w-5xl">
        <!-- Back link -->
        <a href="/trucking-guide"
            class="inline-flex items-center gap-2 text-sm sm:text-base text-blue-600 hover:text-blue-700 mb-4">
            <span>←</span> Back to Trucking Guide
        </a>

        <!-- Title -->
        <h1 class="text-4xl font-bold text-gray-900 mb-4">
            🎯 Step 3: Reusable Components
        </h1>

        <!-- Description -->
        <p class="text-xl text-gray-600 mb-8">
            Build reusable components that accept parameters from parent components, enabling code reuse and
            composition.
        </p>

        <!-- What You'll Learn (Gradient Banner) -->
        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">What You'll Learn</h2>

            <p class="text-gray-700 mb-4 text-sm sm:text-base">
                Components are the building blocks of Blazor apps. You can create reusable components that accept
                parameters, making your code modular and maintainable. This step teaches you how to use the [Parameter]
                attribute to create flexible, reusable UI components.
            </p>

            <ul class="list-disc ml-6 text-sm sm:text-base text-gray-700 space-y-2 mb-4">
                <li>Use [Parameter] attribute to mark properties that can be set from parent components</li>
                <li>Set default values for parameters to make components flexible</li>
                <li>Use components like HTML tags with custom attributes</li>
                <li>Build maintainable code through component reuse and composition</li>
            </ul>

            <div class="mt-2 pt-3 border-t border-blue-200">
                <p class="text-xs sm:text-sm text-gray-700 mb-2">
                    <strong>💡 Key Concepts:</strong> [Parameter] Attribute, Component Reuse, Property Binding, Default
                    Values
                </p>
                <div class="flex flex-wrap items-center gap-2">
                    <span class="text-xs sm:text-sm text-gray-600">→ Related tips:</span>
                    <a href="/tips#parameter"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">[Parameter]</a>
                </div>
            </div>
        </div>

        <!-- Sample Component (White Card) -->
        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl font-bold text-gray-900 mb-4">🧩 Code Example: GreetingCard Component</h2>

            <p class="text-sm text-gray-600 mb-4">
                This component demonstrates how to create a reusable component with parameters. The GreetingCard accepts
                Name and Message parameters, making it flexible for different use cases.
            </p>

            <!-- Full code example -->
            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mb-4">
                <div class="text-xs text-gray-500 mb-1">GreetingCard.razor</div>
                <p class="text-xs text-gray-600 mb-2">A reusable component that displays personalized greeting cards
                    with customizable name and message.</p>
                <ul class="list-disc ml-4 mb-3 text-[11px] text-gray-600 space-y-1">
                    <li>[Parameter] attribute makes Name and Message properties settable from parent components</li>
                    <li>Default values ("Guest", "Welcome...") provide fallback content</li>
                    <li>Component can be used multiple times with different parameter values</li>
                    <li>DateTime.Now shows when the card was created</li>
                </ul>
                <pre data-code-title="Razor (GreetingCard.razor)"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">@@* GreetingCard.razor - Reusable component with parameters *@@

&lt;div class="bg-white rounded-2xl duration-300 p-5 sm:p-6 border border-gray-200"&gt;
    &lt;div class="flex items-center mb-4 gap-3"&gt;
        &lt;div class="bg-orange-500 text-white rounded-full p-2 flex-shrink-0"&gt;
            &lt;svg class="w-5 h-5 sm:w-6 sm:h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"&gt;
                &lt;path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"&gt;&lt;/path&gt;
            &lt;/svg&gt;
        &lt;/div&gt;
        &lt;h3 class="text-lg sm:text-xl font-semibold text-gray-900 break-words min-w-0"&gt;Hello, @@Name!&lt;/h3&gt;
    &lt;/div&gt;

    &lt;p class="text-sm sm:text-base text-gray-600 leading-relaxed mb-4 break-words"&gt;
        @@Message
    &lt;/p&gt;

    &lt;div class="flex items-center text-xs sm:text-sm text-gray-500"&gt;
        &lt;svg class="w-4 h-4 mr-1 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"&gt;
            &lt;path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"&gt;&lt;/path&gt;
        &lt;/svg&gt;
        &lt;span class="break-words"&gt;Created on @@DateTime.Now.ToString("MMM dd, yyyy")&lt;/span&gt;
    &lt;/div&gt;
&lt;/div&gt;

@@code {
    /// &lt;summary&gt;
    /// The name to display in the greeting card
    /// &lt;/summary&gt;
    [Parameter]
    public string Name { get; set; } = "Guest";

    /// &lt;summary&gt;
    /// The message content to display in the card
    /// &lt;/summary&gt;
    [Parameter]
    public string Message { get; set; } = "Welcome to our application!";
}</pre>
            </div>

            <!-- Usage example -->
            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mb-4">
                <div class="text-xs text-gray-500 mb-1">Usage in any page</div>
                <p class="text-xs text-gray-600 mb-2">How to use the GreetingCard component with different parameter
                    values.</p>
                <ul class="list-disc ml-4 mb-3 text-[11px] text-gray-600 space-y-1">
                    <li>Pass Name and Message as HTML-style attributes</li>
                    <li>Each instance can have different values</li>
                    <li>Grid layout displays cards responsively</li>
                </ul>
                <pre data-code-title="Razor (Usage)"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">&lt;div class="grid md:grid-cols-2 gap-4"&gt;
    &lt;GreetingCard Name="John Doe" Message="Welcome to your dashboard!" /&gt;
    &lt;GreetingCard Name="Sarah Smith" Message="Great job on your delivery!" /&gt;
&lt;/div&gt;</pre>
            </div>

            <!-- Step-by-step breakdown -->
            <h3 class="text-sm font-semibold text-gray-800 mb-2">Step-by-step breakdown</h3>
            <div class="grid gap-4">
                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4">
                    <div class="text-xs text-gray-500 mb-1">1. Component layout and styling</div>
                    <p class="text-xs text-gray-600 mb-2">The card container uses Tailwind CSS for responsive,
                        consistent styling.</p>
                    <ul class="list-disc ml-4 mb-2 text-[11px] text-gray-600 space-y-1">
                        <li>Rounded corners and border create card appearance</li>
                        <li>Padding and spacing ensure content breathes</li>
                        <li>Responsive classes adapt to screen sizes</li>
                    </ul>
                    <pre
                        class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">&lt;div class="bg-white rounded-2xl duration-300 p-5 sm:p-6 border border-gray-200"&gt;
    &lt;!-- Card content --&gt;
&lt;/div&gt;</pre>
                </div>

                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4">
                    <div class="text-xs text-gray-500 mb-1">2. Displaying parameter values</div>
                    <p class="text-xs text-gray-600 mb-2">The @@Name and @@Message syntax renders the parameter values.
                    </p>
                    <ul class="list-disc ml-4 mb-2 text-[11px] text-gray-600 space-y-1">
                        <li>@@Name is inserted into the heading</li>
                        <li>@@Message fills the paragraph content</li>
                        <li>If no value provided, defaults are used</li>
                    </ul>
                    <pre
                        class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">&lt;h3 class="text-lg sm:text-xl font-semibold text-gray-900"&gt;Hello, @@Name!&lt;/h3&gt;
&lt;p class="text-sm sm:text-base text-gray-600"&gt;@@Message&lt;/p&gt;</pre>
                </div>

                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4">
                    <div class="text-xs text-gray-500 mb-1">3. [Parameter] attribute</div>
                    <p class="text-xs text-gray-600 mb-2">Properties marked with [Parameter] can receive values from
                        parent components.</p>
                    <ul class="list-disc ml-4 mb-2 text-[11px] text-gray-600 space-y-1">
                        <li>Public properties with [Parameter] become component parameters</li>
                        <li>Default values provide fallback when not specified</li>
                        <li>Property types determine what values are accepted</li>
                    </ul>
                    <pre
                        class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">[Parameter]
public string Name { get; set; } = "Guest";

[Parameter]
public string Message { get; set; } = "Welcome to our application!";</pre>
                </div>
            </div>
        </div>

        <!-- Important Notes & Tips (White Card) -->
        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">📝 Important Notes & Tips</h2>

            <div class="space-y-3">
                <!-- Pro Tips (Blue) -->
                <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                    <h3 class="text-sm font-bold text-blue-900 mb-3">💡 Pro Tips</h3>
                    <div class="space-y-4">
                        <div>
                            <p class="text-sm font-semibold text-blue-900 mb-1">Always provide default values</p>
                            <p class="text-xs text-blue-800">Default parameter values make components more resilient and
                                easier to use without requiring all parameters.</p>
                        </div>
                        <div>
                            <p class="text-sm font-semibold text-blue-900 mb-1">Use [Parameter] for public properties
                                only</p>
                            <p class="text-xs text-blue-800">Only public properties can be parameters. Private fields
                                and properties won't work.</p>
                        </div>
                        <div>
                            <p class="text-sm font-semibold text-blue-900 mb-1">Component file location matters</p>
                            <p class="text-xs text-blue-800">Place components in Components/ folder or Pages/ folder
                                based on whether they're reusable UI elements or routable pages.</p>
                        </div>
                    </div>
                </div>

                <!-- Troubleshooting (Yellow) -->
                <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
                    <h3 class="text-sm font-bold text-yellow-900 mb-2">⚠️ Troubleshooting</h3>
                    <ul class="list-disc ml-6 text-sm text-yellow-900 space-y-1">
                        <li><strong>Component not found?</strong> Check file location and ensure proper namespace.
                            Component name must match filename.</li>
                        <li><strong>[Parameter] not working?</strong> Verify the property is public and has both getter
                            and setter.</li>
                        <li><strong>Parameters not updating?</strong> Make sure you're using attribute syntax:
                            Name="value" not Name=value</li>
                        <li><strong>Compilation error?</strong> Add @@using Microsoft.AspNetCore.Components to access
                            [Parameter] attribute.</li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- How to do it (Gradient Banner) -->
        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">🛠️ How to do it</h2>
            <ol class="list-decimal ml-5 space-y-2 text-gray-700 text-sm sm:text-base">
                <li>
                    <strong>Create GreetingCard.razor:</strong>
                    Create file at <code>Components/GreetingCard.razor</code> (or in your components folder)
                </li>
                <li>
                    <strong>Add component code:</strong>
                    Copy the GreetingCard.razor code from above, including the @@code block with parameters
                </li>
                <li>
                    <strong>Use in Home.razor:</strong>
                    Open <code>Components/Pages/Home.razor</code> and add the usage code
                </li>
                <li>
                    <strong>Test the component:</strong>
                    Run <code>dotnet run</code> and navigate to home page to see your reusable greeting cards
                </li>
                <li>
                    <strong>Experiment:</strong>
                    Try creating more instances with different names and messages
                </li>
            </ol>
        </div>

        <!-- Live Demo Section -->
        <div class="bg-black/3 rounded-2xl p-6 mb-6">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">🎬 Live Demo</h2>
            <p class="text-sm text-gray-600 mb-4">See the GreetingCard component in action with different parameter
                values:</p>

            <div class="grid md:grid-cols-2 gap-4">
                <BlazorMock.Components.GreetingCard Name="John Doe" Message="Welcome to your trucking dashboard!" />
                <BlazorMock.Components.GreetingCard Name="Sarah Smith" Message="Great job on your delivery!" />
            </div>

            <p class="text-xs text-gray-600 mt-4 text-center">💡 Same component, different parameters - that's the power
                of reusability!</p>
        </div>

        <!-- Mark Complete Section (Green Card) -->
        <div class="bg-white rounded-xl p-6 border-2 border-green-200 mb-6">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div>
                    <h3 class="text-lg font-semibold text-gray-900 mb-1">Finished Step 3?</h3>
                    <p class="text-sm text-gray-600">Mark this step complete to track your progress and continue
                        learning.</p>
                </div>
                @if (isComplete)
                {
                    <div class="flex items-center gap-3">
                        <span class="px-4 py-2 rounded-full bg-green-100 text-green-700 font-medium">✓ Completed</span>
                        <button @onclick="ResetStep"
                            class="px-4 py-2 border-2 border-gray-200 rounded-lg hover:bg-gray-50 transition-colors text-sm">Reset</button>
                    </div>
                }
                else
                {
                    <button @onclick="MarkComplete"
                        class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg font-medium transition-colors">
                        Mark as Complete
                    </button>
                }
            </div>
        </div>

        <!-- Navigation -->
        <div class="flex items-center justify-between">
            <a href="/trucking-examples/step2"
                class="inline-flex items-center gap-2 px-4 py-2 border-2 border-gray-200 rounded-lg hover:bg-gray-50 transition-colors">
                <span>←</span>
                Previous: Step 2
            </a>
            <a href="/trucking-examples/step4"
                class="inline-flex items-center gap-2 px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition-colors">
                Next: Step 4
                <span>→</span>
            </a>
        </div>
    </div>
</div>