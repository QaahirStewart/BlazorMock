@page "/admin-dashboard-examples/step3"
@rendermode InteractiveServer
@inherits BlazorMock.Components.Pages.Examples.AdminDashboard.Step3.ExampleBase

<PageTitle>Admin Dashboard Step 3: Sign Up Page - Example</PageTitle>

<div class="min-h-screen py-8 rounded-4xl bg-gray-50">
    <div class="w-full mx-auto px-4 max-w-5xl">
        <!-- Back link -->
        <a href="/admin-dashboard-guide"
            class="inline-flex items-center gap-2 text-sm sm:text-base text-blue-600 hover:text-blue-700 mb-4">
            <span>←</span> Back to Admin Dashboard Guide
        </a>

        <!-- Title -->
        <h1 class="text-4xl font-bold text-gray-900 mb-4">
            ✍️ Step 3: Let Users Create Accounts (Sign Up Page)
        </h1>

        <!-- Description -->
        <p class="text-xl text-gray-600 mb-8">
            Build a sign-up page where people can create their accounts. It's like setting up the front door of your
            app where new users can walk in!
        </p>

        <!-- What You'll Build Section -->
        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">What You'll Build</h2>

            <ul class="list-disc ml-6 text-sm sm:text-base text-gray-700 space-y-2 mb-4">
                <li>A friendly sign-up form with inputs for name, email, and password</li>
                <li>Two-way data binding so your code automatically updates when users type</li>
                <li>A service that handles all the account creation behind the scenes</li>
                <li>Helpful error messages when something goes wrong, and automatic navigation to the profile page when
                    sign-up succeeds</li>
            </ul>

            <div class="mt-2 pt-3 border-t border-blue-200">
                <p class="text-xs sm:text-sm text-gray-700 mb-2">
                    <strong>💡 What You'll Use:</strong> Blazor data binding, auth service, navigation, error handling
                </p>
                <div class="flex flex-wrap items-center gap-2">
                    <span class="text-xs sm:text-sm text-gray-600">→ Related tips:</span>
                    <a href="/tips#data-annotations"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Data
                        Annotations</a>
                    <a href="/tips#editform-+-validation"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">EditForm
                        + Validation</a>
                    <a href="/tips#savechangesasync"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">SaveChangesAsync</a>
                </div>
            </div>
        </div>

        <!-- Signup Form Example -->
        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl font-bold text-gray-900 mb-4">🧩 Sample Component: SignUp.razor (/signup)</h2>

            <p class="text-sm text-gray-600 mb-4">
                This is the exact <code>SignUp.razor</code> file used by the Admin Dashboard demo. You can copy and
                paste it into your own app to get the same experience.
            </p>

            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mb-4">
                <div class="text-xs text-gray-500 mb-1">Components/Pages/Demo/DashboardDemo/Auth/SignUp.razor</div>
                <p class="text-xs text-gray-600 mb-2">Blazor sign up page with a two-column layout and demo accounts
                    sidebar, wired to the shared auth service.</p>
                <ul class="list-disc ml-4 mb-3 text-[11px] text-gray-600 space-y-1">
                    <li>Uses basic inputs with <code>@@bind</code> instead of <code>EditForm</code> for simplicity.</li>
                    <li>Calls into <code>IUserAuthService</code> to perform the actual sign up.</li>
                    <li>Shows an inline error message when something goes wrong.</li>
                    <li>Highlights the three demo accounts your learners can use.</li>
                    <li>Automatically seeds those demo accounts so they exist the moment you run a new project.</li>
                </ul>
                <pre data-code-title="Components/Pages/Demo/DashboardDemo/Auth/SignUp.razor"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">@@page "/signup"
@@rendermode InteractiveServer
@@using AdminDashboard.Components.Pages.Auth
@@inherits SignUpBase

&lt;PageTitle&gt;Sign Up&lt;/PageTitle&gt;

&lt;div class=&quot;py-10 px-4 rounded-3xl bg-gray-50&quot;&gt;
    &lt;div class=&quot;max-w-6xl mx-auto px-4&quot;&gt;
        &lt;div class=&quot;grid grid-cols-1 lg:grid-cols-2 gap-6 items-start&quot;&gt;
            &lt;!-- Left: Sign Up Form --&gt;
            &lt;div class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-6 sm:p-8&quot;&gt;
                &lt;div class=&quot;text-center mb-6&quot;&gt;
                    &lt;div class=&quot;h-16 w-16 bg-emerald-600 rounded-full flex items-center justify-center mx-auto mb-4&quot;&gt;
                        &lt;svg class=&quot;w-8 h-8 text-white&quot; fill=&quot;none&quot; stroke=&quot;currentColor&quot; viewBox=&quot;0 0 24 24&quot;&gt;
                            &lt;path stroke-linecap=&quot;round&quot; stroke-linejoin=&quot;round&quot; stroke-width=&quot;2&quot;
                                d=&quot;M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z&quot; /&gt;
                        &lt;/svg&gt;
                    &lt;/div&gt;
                    &lt;h1 class=&quot;text-3xl font-bold text-gray-900&quot;&gt;Create Account&lt;/h1&gt;
                    &lt;p class=&quot;text-gray-600 mt-2&quot;&gt;Start your journey with us&lt;/p&gt;
                &lt;/div&gt;

                @@if (!string.IsNullOrEmpty(errorMessage))
                {
                    &lt;div class=&quot;mb-4 p-3 bg-red-50 border border-red-200 rounded-lg text-red-700 text-sm&quot;&gt;
                        @@errorMessage
                    &lt;/div&gt;
                }

                &lt;div class=&quot;space-y-4&quot;&gt;
                    &lt;div&gt;
                        &lt;label class=&quot;block text-sm font-medium text-gray-700 mb-2&quot;&gt;Full Name&lt;/label&gt;
                        &lt;input type=&quot;text&quot; @@bind=&quot;fullName&quot;
                            class=&quot;w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-emerald-500&quot;
                            placeholder=&quot;John Doe&quot; /&gt;
                    &lt;/div&gt;
                    &lt;div&gt;
                        &lt;label class=&quot;block text-sm font-medium text-gray-700 mb-2&quot;&gt;Email&lt;/label&gt;
                        &lt;input type=&quot;email&quot; @@bind=&quot;email&quot;
                            class=&quot;w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-emerald-500&quot;
                            placeholder=&quot;you@example.com&quot; /&gt;
                    &lt;/div&gt;
                    &lt;div&gt;
                        &lt;label class=&quot;block text-sm font-medium text-gray-700 mb-2&quot;&gt;Password&lt;/label&gt;
                        &lt;input type=&quot;password&quot; @@bind=&quot;password&quot;
                            @@onkeydown=&quot;@@(e =&gt; { if (e.Key == &quot;Enter&quot;) HandleSignUp(); })&quot;
                            class=&quot;w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-emerald-500&quot;
                            placeholder=&quot;Create a password&quot; /&gt;
                    &lt;/div&gt;
                &lt;/div&gt;

                &lt;button @@onclick=&quot;HandleSignUp&quot;
                    class=&quot;w-full mt-6 bg-emerald-600 hover:bg-emerald-700 text-white font-semibold py-3 rounded-lg transition-colors&quot;&gt;
                    Create Account
                &lt;/button&gt;

                &lt;div class=&quot;mt-6 text-center&quot;&gt;
                    &lt;p class=&quot;text-sm text-gray-600&quot;&gt;
                        Already have an account?
                        &lt;a href=&quot;/signin&quot; class=&quot;text-emerald-600 hover:text-emerald-700 font-medium&quot;&gt;Sign In&lt;/a&gt;
                    &lt;/p&gt;
                &lt;/div&gt;
            &lt;/div&gt;

            &lt;!-- Right: Demo Accounts --&gt;
            &lt;div
                class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-6 sm:p-8 flex-1 h-full flex items-center flex-col justify-center&quot;&gt;
                &lt;h3 class=&quot;text-xl font-bold text-gray-900 mb-2&quot;&gt;Demo Accounts&lt;/h3&gt;
                &lt;p class=&quot;text-sm text-gray-600 mb-6&quot;&gt;Try our platform with these demo accounts&lt;/p&gt;

                &lt;div class=&quot;space-y-3&quot;&gt;
                    &lt;!-- Admin Account --&gt;
                    &lt;div class=&quot;w-full p-4 bg-white border-2 border-gray-200 rounded-xl&quot;&gt;
                        &lt;div class=&quot;flex items-center justify-center gap-3&quot;&gt;
                            &lt;div
                                class=&quot;h-10 w-10 rounded-full bg-red-600 flex items-center justify-center text-white font-semibold shrink-0&quot;&gt;
                                AD
                            &lt;/div&gt;
                            &lt;div class=&quot;flex-1 min-w-0&quot;&gt;
                                &lt;div class=&quot;flex items-center gap-2 mb-1&quot;&gt;
                                    &lt;p class=&quot;text-sm font-semibold text-gray-900&quot;&gt;Admin User&lt;/p&gt;
                                    &lt;span
                                        class=&quot;px-2 py-0.5 text-xs font-medium bg-red-100 text-red-800 rounded&quot;&gt;Admin&lt;/span&gt;
                                &lt;/div&gt;
                                &lt;p class=&quot;text-xs text-gray-600 mb-1&quot;&gt;admin@demo.com&lt;/p&gt;
                                &lt;p class=&quot;text-xs text-gray-500&quot;&gt;Full system access • User management&lt;/p&gt;
                            &lt;/div&gt;
                        &lt;/div&gt;
                    &lt;/div&gt;

                    &lt;!-- Paid Account --&gt;
                    &lt;div class=&quot;w-full p-4 bg-white border-2 border-gray-200 rounded-xl&quot;&gt;
                        &lt;div class=&quot;flex items-center justify-center gap-3&quot;&gt;
                            &lt;div
                                class=&quot;h-10 w-10 rounded-full bg-purple-600 flex items-center justify-center text-white font-semibold shrink-0&quot;&gt;
                                PU
                            &lt;/div&gt;
                            &lt;div class=&quot;flex-1 min-w-0&quot;&gt;
                                &lt;div class=&quot;flex items-center gap-2 mb-1&quot;&gt;
                                    &lt;p class=&quot;text-sm font-semibold text-gray-900&quot;&gt;Paid User&lt;/p&gt;
                                    &lt;span
                                        class=&quot;px-2 py-0.5 text-xs font-medium bg-purple-100 text-purple-800 rounded&quot;&gt;Paid&lt;/span&gt;
                                &lt;/div&gt;
                                &lt;p class=&quot;text-xs text-gray-600 mb-1&quot;&gt;paid@demo.com&lt;/p&gt;
                                &lt;p class=&quot;text-xs text-gray-500&quot;&gt;Advanced analytics • Premium features&lt;/p&gt;
                            &lt;/div&gt;
                        &lt;/div&gt;
                    &lt;/div&gt;

                    &lt;!-- Free Account --&gt;
                    &lt;div class=&quot;w-full p-4 bg-white border-2 border-gray-200 rounded-xl&quot;&gt;
                        &lt;div class=&quot;flex items-center justify-center gap-3&quot;&gt;
                            &lt;div
                                class=&quot;h-10 w-10 rounded-full bg-gray-600 flex items-center justify-center text-white font-semibold shrink-0&quot;&gt;
                                FU
                            &lt;/div&gt;
                            &lt;div class=&quot;flex-1 min-w-0&quot;&gt;
                                &lt;div class=&quot;flex items-center gap-2 mb-1&quot;&gt;
                                    &lt;p class=&quot;text-sm font-semibold text-gray-900&quot;&gt;Free User&lt;/p&gt;
                                    &lt;span
                                        class=&quot;px-2 py-0.5 text-xs font-medium bg-gray-100 text-gray-800 rounded&quot;&gt;Free&lt;/span&gt;
                                &lt;/div&gt;
                                &lt;p class=&quot;text-xs text-gray-600 mb-1&quot;&gt;user@demo.com&lt;/p&gt;
                                &lt;p class=&quot;text-xs text-gray-500&quot;&gt;Basic metrics • Limited access&lt;/p&gt;
                            &lt;/div&gt;
                        &lt;/div&gt;
                    &lt;/div&gt;
                &lt;/div&gt;

                &lt;div class=&quot;mt-6 text-center&quot;&gt;
                    &lt;a href=&quot;/signin&quot;
                        class=&quot;inline-flex items-center gap-2 text-sm text-emerald-600 hover:text-emerald-700 font-medium&quot;&gt;
                        &lt;svg class=&quot;w-4 h-4&quot; fill=&quot;none&quot; stroke=&quot;currentColor&quot; viewBox=&quot;0 0 24 24&quot;&gt;
                            &lt;path stroke-linecap=&quot;round&quot; stroke-linejoin=&quot;round&quot; stroke-width=&quot;2&quot;
                                d=&quot;M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1&quot; /&gt;
                        &lt;/svg&gt;
                        Sign in with demo account
                    &lt;/a&gt;
                &lt;/div&gt;
            &lt;/div&gt;
        &lt;/div&gt;
    &lt;/div&gt;
&lt;/div&gt;</pre>
            </div>
            <p class="text-xs text-gray-600">
                This is a demo-only implementation. In a real app, you would hash passwords, use a proper identity
                system, and store users in a real database.
            </p>

            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mt-6">
                <div class="text-xs text-gray-500 mb-1">Components/Pages/Demo/DashboardDemo/Auth/SignUp.razor.cs</div>
                <p class="text-xs text-gray-600 mb-2">Code-behind base class that injects <code>IUserAuthService</code>
                    and handles <code>HandleSignUp</code>.</p>
                <pre data-code-title="Components/Pages/Demo/DashboardDemo/Auth/SignUp.razor.cs"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">using Microsoft.AspNetCore.Components;
using AdminDashboard.Services;

namespace AdminDashboard.Components.Pages.Auth;

public partial class SignUpBase : ComponentBase
{
    [Inject] protected IUserAuthService Auth { get; set; } = default!;
    [Inject] protected NavigationManager Nav { get; set; } = default!;

    protected string fullName = string.Empty;
    protected string email = string.Empty;
    protected string password = string.Empty;
    protected string errorMessage = string.Empty;

    protected void HandleSignUp()
    {
        errorMessage = string.Empty;

        if (string.IsNullOrWhiteSpace(fullName) || string.IsNullOrWhiteSpace(email) || string.IsNullOrWhiteSpace(password))
        {
            errorMessage = "Please fill in all fields.";
            return;
        }

        if (Auth.SignUp(email, password, fullName))
        {
            Nav.NavigateTo("/profile");
        }
        else
        {
            errorMessage = "An account with this email already exists.";
        }
    }
}
</pre>
            </div>

            <div class="mt-6 space-y-4">
                <h3 class="text-lg font-semibold text-gray-900">Auth service + DI setup</h3>
                <p class="text-xs text-gray-600">These files unblock the <code>AdminDashboard.Services</code> namespace
                    and keep the demo self-contained.</p>

                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto">
                    <div class="text-xs text-gray-500 mb-1">Services/IUserAuthService.cs</div>
                    <pre data-code-title="Services/IUserAuthService.cs"
                        class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">namespace AdminDashboard.Services;

public interface IUserAuthService
{
    bool SignUp(string email, string password, string fullName);
    bool SignIn(string email, string password);
}</pre>
                </div>

                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto">
                    <div class="text-xs text-gray-500 mb-1">Services/DemoUserAuthService.cs</div>
                    <p class="text-xs text-gray-600 mb-2">EF Core-backed implementation that writes to the
                        <code>Users</code> table created in Step 2 and seeds the three demo accounts automatically.
                    </p>
                    <pre data-code-title="Services/DemoUserAuthService.cs"
                        class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">using System.Linq;
using AdminDashboard.Data;
using AdminDashboard.Models;

namespace AdminDashboard.Services;

public class DemoUserAuthService : IUserAuthService
{
    private readonly AppDbContext _db;

    public DemoUserAuthService(AppDbContext db)
    {
        _db = db;
        EnsureDemoUsers();
    }

    public bool SignUp(string email, string password, string fullName)
    {
        var normalizedEmail = email.Trim().ToLowerInvariant();

        if (_db.Users.Any(u =&gt; u.Email == normalizedEmail))
        {
            return false;
        }

        var user = new User
        {
            Email = normalizedEmail,
            PasswordHash = password,
            FullName = string.IsNullOrWhiteSpace(fullName) ? "New User" : fullName.Trim(),
            Role = "Viewer"
        };

        _db.Users.Add(user);
        _db.SaveChanges();
        return true;
    }

    public bool SignIn(string email, string password)
    {
        var normalizedEmail = email.Trim().ToLowerInvariant();
        var user = _db.Users.FirstOrDefault(u =&gt; u.Email == normalizedEmail);
        return user is not null && user.PasswordHash == password;
    }

    private void EnsureDemoUsers()
    {
        var demoUsers = new[]
        {
            new User { Email = "admin@demo.com", PasswordHash = "admin123", FullName = "Admin User", Role = "Admin" },
            new User { Email = "paid@demo.com", PasswordHash = "paid123", FullName = "Paid User", Role = "Paid" },
            new User { Email = "user@demo.com", PasswordHash = "user123", FullName = "Free User", Role = "Free" }
        };

        var addedAny = false;

        foreach (var demo in demoUsers)
        {
            var normalizedEmail = demo.Email.Trim().ToLowerInvariant();
            if (_db.Users.Any(u =&gt; u.Email == normalizedEmail))
            {
                continue;
            }

            demo.Email = normalizedEmail;
            _db.Users.Add(demo);
            addedAny = true;
        }

        if (addedAny)
        {
            _db.SaveChanges();
        }
    }
}</pre>
                </div>
                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto">
                    <div class="text-xs text-gray-500 mb-1">Program.cs (DI registration)</div>
                    <p class="text-xs text-gray-600 mb-2">Add this after the existing
                        <code>AddDbContext&lt;AppDbContext&gt;</code> call.
                    </p>
                    <pre data-code-title="Program.cs (snippet)"
                        class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">using AdminDashboard.Services;

builder.Services.AddScoped&lt;IUserAuthService, DemoUserAuthService&gt;();</pre>
                </div>
            </div>
        </div>

        <!-- Important Notes & Tips Section -->
        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">📝 Important Notes &amp; Tips</h2>

            <div class="space-y-3">
                <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
                    <h3 class="text-sm font-bold text-yellow-900 mb-2">Set up the auth service before pasting</h3>
                    <p class="text-xs sm:text-sm text-yellow-900 mb-3">
                        The <code>SignUp</code> component injects <code>IUserAuthService</code> from
                        <code>AdminDashboard.Services</code>. Create the service files and register them in DI first,
                        otherwise you'll hit <code>CS0234</code>/<code>CS0246</code> errors.
                    </p>
                    <ul class="list-disc ml-5 text-xs sm:text-sm text-yellow-900 space-y-1">
                        <li>Add <code>Services/IUserAuthService.cs</code> (interface).</li>
                        <li>Add <code>Services/DemoUserAuthService.cs</code> (EF-backed implementation).</li>
                        <li>Update <code>Program.cs</code>:
                            <code>builder.Services.AddScoped&lt;IUserAuthService, DemoUserAuthService&gt;();</code>.
                        </li>
                    </ul>
                    <p class="text-xs text-yellow-900 mt-3">Full snippets are in the "Auth service + DI setup" card
                        above.</p>
                </div>

                <!-- Existing/general tips placeholder if needed -->
                <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                    <h3 class="text-sm font-bold text-blue-900 mb-2">Other handy reminders</h3>
                    <ul class="list-disc ml-5 text-xs sm:text-sm text-blue-900 space-y-1">
                        <li>Stick with the namespace from your project (`AdminDashboard` is just a placeholder).</li>
                        <li>Keep using the Step 2 `AppDbContext`/`User` classes so the sample service compiles.</li>
                        <li>Once things build, run `dotnet ef database update` again if you've added new migrations.
                        </li>
                        <li>Copying straight from GitHub shows `@@` because the docs escape Razor syntax—replace `@@`
                            with a single <code>&#64;</code> if you paste from the raw file instead of the running site.
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Step-by-Step Guide (Gradient Banner) -->
        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">🛠️ Step-by-Step Guide</h2>
            <ol class="list-decimal ml-5 space-y-2 text-gray-700 text-sm sm:text-base">
                <li>
                    <strong>Create your sign-up page:</strong>
                    Add a new file <code>Components/Pages/SignUp.razor</code> (or wherever makes sense in your project).
                </li>
                <li>
                    <strong>Set up the basics:</strong>
                    Add <code>@@page "/signup"</code> at the top so people can visit your sign-up page, and use
                    <code>@@rendermode InteractiveServer</code> to make it interactive.
                </li>
                <li>
                    <strong>Create the form inputs:</strong>
                    Add input fields for name, email, and password. Use <code>@@bind</code> to automatically connect
                    them
                    to your component's code.
                </li>
                <li>
                    <strong>Add the sign-up button:</strong>
                    Create a button that calls your <code>HandleSignUp</code> method when clicked. This is where the
                    magic happens!
                </li>
                <li>
                    <strong>Connect the auth service:</strong>
                    Inject <code>IUserAuthService</code> in your code-behind file and call it to actually create the
                    account. If it succeeds, navigate to <code>/profile</code>. If not, show an error message.
                </li>
                <li>
                    <strong>Link to sign in:</strong>
                    Add the "Already have an account? Sign In" link to <code>/signin</code> so learners can move
                    between the two auth pages.
                </li>
                <li>
                    <strong>Test the flow:</strong>
                    Run the app, go to <code>/signup</code>, try creating an account, and confirm that you're
                    redirected to <code>/profile</code>.
                </li>
                <li>
                    <strong>Connect to the rest of the app:</strong>
                    After this step, you'll build the real <code>/signin</code> page in Step 4 to complete the auth
                    loop.
                </li>
            </ol>
        </div>

        <!-- Live Demo -->
        <div class="bg-black/3 rounded-2xl p-6 mb-6">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">🎬 Live Demo</h2>
            <p class="text-sm text-gray-600 mb-4">
                Here's what the <code>/signup</code> page looks like:
            </p>
            <div class="bg-white rounded-2xl border border-gray-200 overflow-hidden">
                <div class="max-w-md mx-auto p-8">
                    <h2 class="text-3xl font-bold text-gray-900 mb-2">Create Account</h2>
                    <p class="text-gray-600 mb-6">Sign up to get started</p>

                    <div class="space-y-4">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                            <input type="email" placeholder="you@example.com"
                                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
                        </div>

                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Password</label>
                            <input type="password" placeholder="••••••••"
                                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
                        </div>

                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Role (optional)</label>
                            <input type="text" placeholder="User"
                                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
                        </div>

                        <button
                            class="w-full py-3 bg-blue-600 text-white rounded-lg font-semibold hover:bg-blue-700 transition-colors">
                            Sign Up
                        </button>
                    </div>

                    <p class="text-sm text-gray-600 mt-4 text-center">
                        Already have an account? <a href="/signin"
                            class="text-blue-600 hover:text-blue-700 font-medium">Sign in</a>
                    </p>
                </div>

                <div class="bg-gray-50 border-t border-gray-200 px-6 py-4">
                    <p class="text-xs text-gray-600">
                        💡 <strong>Try it live:</strong> <a href="/signup"
                            class="text-blue-600 hover:text-blue-700 font-medium">Open /signup →</a> to test the real
                        page with validation and database writes.
                    </p>
                </div>
            </div>
        </div>

        <!-- Mark Complete Section -->
        <div class="bg-white rounded-xl p-6 border-2 border-green-200 mb-6">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div>
                    <h3 class="text-lg font-semibold text-gray-900 mb-1">Finished Step 3?</h3>
                    <p class="text-sm text-gray-600">Mark this step as complete to track your Admin Dashboard learning
                        progress.</p>
                </div>
                @if (isComplete)
                {
                    <div class="flex items-center gap-3">
                        <span class="px-4 py-2 rounded-full bg-green-100 text-green-700 font-medium">✓ Completed</span>
                        <button @onclick="ResetStep"
                            class="px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors text-sm">Reset</button>
                    </div>
                }
                else
                {
                    <button @onclick="MarkComplete"
                        class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg font-medium transition-colors">
                        Mark as Complete
                    </button>
                }
            </div>
        </div>

        <!-- Navigation -->
        <div class="mt-8">
            <div class="bg-black/90 rounded-2xl p-6 sm:p-8 text-white text-center">
                <p class="text-lg sm:text-xl font-semibold mb-4">Continue Learning</p>
                <p class="text-sm text-gray-300 mb-6 max-w-2xl mx-auto">
                    Great! With <code>/signup</code> in place, you're ready to build the <code>/signin</code> page and
                    complete the basic auth loop for your Admin Dashboard.
                </p>
                <div class="flex flex-col sm:flex-row items-center justify-center gap-3">
                    <a href="/admin-dashboard-guide"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 rounded-xl transition-colors">
                        <span class="text-lg">←</span>
                        <span class="text-sm sm:text-base font-semibold">Back to Guide</span>
                    </a>
                    <a href="/admin-dashboard-examples/step2"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 rounded-xl transition-colors">
                        <span class="text-lg">←</span>
                        <span class="text-sm sm:text-base font-semibold">Previous: Step 2</span>
                    </a>
                    <a href="/admin-dashboard-examples/step4"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700 rounded-xl transition-colors">
                        <span class="text-sm sm:text-base font-semibold">Next: Step 4</span>
                        <span class="text-lg">→</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>