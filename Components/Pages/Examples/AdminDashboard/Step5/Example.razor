@page "/admin-dashboard-examples/step5"
@rendermode InteractiveServer
@inherits ExampleBase

<PageTitle>Admin Dashboard Step 5: Profile Page - Example</PageTitle>

<div class="min-h-screen py-8 rounded-4xl bg-gray-50">
    <div class="w-full mx-auto px-4 max-w-5xl">
        <a href="/admin-dashboard-guide"
            class="inline-flex items-center gap-2 text-sm sm:text-base text-blue-600 hover:text-blue-700 mb-4">
            <span>←</span> Back to Admin Dashboard Guide
        </a>

        <h1 class="text-4xl font-bold text-gray-900 mb-4">
            🧑‍💼 Step 5: Build the /profile page (Profile.razor)
        </h1>

        <p class="text-xl text-gray-600 mb-8">
            Build the full <code>/profile</code> experience so learners can manage their name, profile photo, and plan
            status using the shared <code>IUserAuthService</code> inside a brand-new Blazor app.
        </p>

        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">What You'll Learn</h2>
            <ul class="list-disc ml-6 text-sm sm:text-base text-gray-700 space-y-2 mb-4">
                <li>Gate the page with <code>ProfileBase</code> so unauthenticated users get redirected to
                    <code>/signin</code>.</li>
                <li>Bind directly to the properties exposed on <code>IUserAuthService.CurrentUser</code>.</li>
                <li>Implement profile photo updates plus optimistic success messaging.</li>
                <li>Surface account stats (member since, last login, role) from the canonical data model.</li>
                <li>Trigger role upgrades (free → paid) using the shared auth service.</li>
            </ul>
            <div class="mt-2 pt-3 border-t border-blue-200">
                <p class="text-xs sm:text-sm text-gray-700 mb-2">
                    <strong>💡 Key Concepts:</strong> Auth-gated pages, shared service state, optimistic UI updates, and
                    Tailwind layout polish.
                </p>
                <div class="flex flex-wrap items-center gap-2">
                    <span class="text-xs sm:text-sm text-gray-600">→ Related tips:</span>
                    <a href="/tips#auth-service"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Auth
                        Service</a>
                    <a href="/tips#layout-state"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Layout
                        State</a>
                    <a href="/tips#tailwind"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Tailwind
                        Layouts</a>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl font-bold text-gray-900 mb-4">🧩 Sample Component: Profile.razor (/profile)</h2>
            <p class="text-sm text-gray-600 mb-4">
                Drop in the exact markup from the demo so your tutorial stays pixel-perfect with the canonical profile
                page.
            </p>
            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mb-4">
                <div class="text-xs text-gray-500 mb-1">Components/Pages/Demo/DashboardDemo/Profile.razor</div>
                <p class="text-xs text-gray-600 mb-2">Auth-gated profile editor that uses <code>ProfileBase</code> plus
                    <code>IUserAuthService</code> helpers.</p>
                <ul class="list-disc ml-4 mb-3 text-[11px] text-gray-600 space-y-1">
                    <li>Redirects to <code>/signin</code> if no user is present.</li>
                    <li>Includes profile photo upload-by-URL, name editing, save/upgrade CTA, and account stats grid.
                    </li>
                    <li>All interactivity funnels through the shared auth service so no EF Core code lives in the page.
                    </li>
                </ul>
                <pre data-code-title="Components/Pages/Demo/DashboardDemo/Profile.razor"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">@@page "/profile"
@@rendermode InteractiveServer
@@inherits ProfileBase

&lt;PageTitle&gt;My Profile&lt;/PageTitle&gt;

@@if (Auth.CurrentUser == null)
{
    Nav.NavigateTo("/signin");
}
else
{
    &lt;div class=&quot; py-10 px-4 bg-gray-50 rounded-3xl&quot;&gt;
        &lt;div class=&quot;max-w-5xl mx-auto px-4 space-y-6&quot;&gt;
            &lt;div class=&quot;text-center mb-8&quot;&gt;
                &lt;h1 class=&quot;text-3xl sm:text-4xl font-bold text-gray-900 mb-2&quot;&gt;My Profile&lt;/h1&gt;
                &lt;p class=&quot;text-base text-gray-600&quot;&gt;Manage your account settings and preferences&lt;/p&gt;
            &lt;/div&gt;

            &lt;div class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-6 sm:p-8&quot;&gt;
                &lt;div class=&quot;flex flex-col md:flex-row gap-8 items-start&quot;&gt;
                    &lt;div class=&quot;flex flex-col items-center gap-4 w-full md:w-auto&quot;&gt;
                        &lt;div class=&quot;relative&quot;&gt;
                            @@if (!string.IsNullOrEmpty(Auth.CurrentUser.ProfilePictureUrl))
                            {
                                &lt;img src=&quot;@@Auth.CurrentUser.ProfilePictureUrl&quot; alt=&quot;Profile&quot;
                                    class=&quot;h-32 w-32 rounded-full object-cover border-2 border-gray-200&quot; /&gt;
                            }
                            else
                            {
                                &lt;div
                                    class=&quot;h-32 w-32 rounded-full  flex items-center justify-center text-black text-4xl font-bold border-2 border-gray-200&quot;&gt;
                                    @@GetInitials()
                                &lt;/div&gt;
                            }
                        &lt;/div&gt;
                        &lt;div class=&quot;w-full md:w-64 space-y-3&quot;&gt;
                            &lt;input type=&quot;text&quot; @@bind=&quot;profilePictureUrl&quot; placeholder=&quot;Enter image URL&quot;
                                class=&quot;w-full px-3 py-2 text-sm border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500&quot; /&gt;
                            &lt;button @@onclick=&quot;UpdateProfilePicture&quot;
                                class=&quot;w-full px-8 py-3 text-sm font-medium bg-blue-600 hover:bg-blue-700 text-white rounded transition-colors&quot;&gt;
                                Update Photo
                            &lt;/button&gt;
                        &lt;/div&gt;
                    &lt;/div&gt;

                    &lt;div class=&quot;flex-1 space-y-5 w-full&quot;&gt;
                        &lt;div&gt;
                            &lt;label class=&quot;block text-sm font-medium text-gray-700 mb-2&quot;&gt;Full Name&lt;/label&gt;
                            &lt;input type=&quot;text&quot; @@bind=&quot;fullName&quot;
                                class=&quot;w-full px-4 py-3 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm&quot; /&gt;
                        &lt;/div&gt;
                        &lt;div&gt;
                            &lt;label class=&quot;block text-sm font-medium text-gray-700 mb-2&quot;&gt;Email&lt;/label&gt;
                            &lt;input type=&quot;email&quot; value=&quot;@@Auth.CurrentUser.Email&quot; disabled
                                class=&quot;w-full px-4 py-3 border border-gray-200 bg-gray-50 rounded text-sm text-gray-500&quot; /&gt;
                        &lt;/div&gt;
                        &lt;div class=&quot;flex flex-col sm:flex-row sm:justify-end items-end sm:items-center gap-3  w-full&quot;&gt;
                            &lt;button @@onclick=&quot;HandleSave&quot;
                                class=&quot;px-8 py-3 bg-white hover:bg-green-500 items-center hover:text-white justify-center flex text-black border border-gray-300 text-sm font-medium rounded transition-colors whitespace-nowrap md:w-34 w-full&quot;&gt;
                                Save Changes
                            &lt;/button&gt;
                            &lt;button @@onclick=&quot;HandleSignOut&quot;
                                class=&quot;px-6 py-3 bg-black hover:bg-gray-800 items-center justify-center flex text-white text-sm font-medium rounded transition-colors whitespace-nowrap md:w-34 w-full&quot;&gt;
                                Sign Out
                            &lt;/button&gt;
                        &lt;/div&gt;
                    &lt;/div&gt;
                &lt;/div&gt;
            &lt;/div&gt;

            &lt;div class=&quot;grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 max-w-5xl mx-auto&quot;&gt;
                &lt;div class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-5 text-center hover:border-blue-400 hover:shadow-lg transition-all duration-200&quot;&gt;
                    &lt;p class=&quot;text-xs font-medium text-gray-500 uppercase mb-2&quot;&gt;Member Since&lt;/p&gt;
                    &lt;p class=&quot;text-xl font-bold text-gray-900&quot;&gt;
                        @@Auth.CurrentUser.CreatedAt.ToString(&quot;MMM dd, yyyy&quot;)
                    &lt;/p&gt;
                &lt;/div&gt;
                &lt;div class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-5 text-center hover:border-blue-400 hover:shadow-lg transition-all duration-200&quot;&gt;
                    &lt;p class=&quot;text-xs font-medium text-gray-500 uppercase mb-2&quot;&gt;Last Login&lt;/p&gt;
                    &lt;p class=&quot;text-xl font-bold text-gray-900&quot;&gt;
                        @@Auth.CurrentUser.LastLoginAt.ToString(&quot;MMM dd, h:mm tt&quot;)
                    &lt;/p&gt;
                &lt;/div&gt;
                &lt;div class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-5 text-center hover:border-blue-400 hover:shadow-lg transition-all duration-200&quot;&gt;
                    &lt;p class=&quot;text-xs font-medium text-gray-500 uppercase mb-2&quot;&gt;Account Status&lt;/p&gt;
                    &lt;p class=&quot;text-xl font-bold text-emerald-600&quot;&gt;Active&lt;/p&gt;
                &lt;/div&gt;
                &lt;div class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-5 text-center hover:border-blue-400 hover:shadow-lg transition-all duration-200&quot;&gt;
                    &lt;p class=&quot;text-xs font-medium text-gray-500 uppercase mb-2&quot;&gt;Account Type&lt;/p&gt;
                    &lt;p class=&quot;text-xl font-bold @@GetRoleColorClass()&quot;&gt;
                        @@Auth.CurrentUser.Role
                    &lt;/p&gt;
                    @@if (Auth.CurrentUser.Role == &quot;Free&quot;)
                    {
                        &lt;button @@onclick=&quot;UpgradeToPaid&quot;
                            class=&quot;mt-3 w-full px-4 py-1.5 text-sm font-medium bg-purple-600 hover:bg-purple-700 text-white rounded transition-colors&quot;&gt;
                            Upgrade to Paid
                        &lt;/button&gt;
                    }
                &lt;/div&gt;
            &lt;/div&gt;

            @@if (saveMessage != null)
            {
                &lt;div
                    class=&quot;bg-emerald-50 border border-emerald-200 rounded p-4 text-emerald-700 text-sm text-center max-w-lg mx-auto&quot;&gt;
                    @@saveMessage
                &lt;/div&gt;
            }
        &lt;/div&gt;
    &lt;/div&gt;
}
</pre>
            </div>
            <p class="text-xs text-gray-600">
                Just like the real app, this version keeps all profile logic in the shared auth service. There’s no EF
                Core or direct database access inside the page.
            </p>

            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mt-6">
                <div class="text-xs text-gray-500 mb-1">Components/Pages/Demo/DashboardDemo/Profile.razor.cs</div>
                <p class="text-xs text-gray-600 mb-2">Code-behind that injects <code>IUserAuthService</code>, handles
                    redirects, profile edits, upgrades, and sign out.</p>
                <pre data-code-title="Components/Pages/Demo/DashboardDemo/Profile.razor.cs"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">using Microsoft.AspNetCore.Components;
using Microsoft.JSInterop;
using AdminDashboard.Services;

namespace AdminDashboard.Components.Pages;

public partial class ProfileBase : ComponentBase
{
    [Inject] protected IUserAuthService Auth { get; set; } = default!;
    [Inject] protected NavigationManager Nav { get; set; } = default!;
    [Inject] protected IJSRuntime JS { get; set; } = default!;

    protected string fullName = string.Empty;
    protected string profilePictureUrl = string.Empty;
    protected string? saveMessage;

    protected override void OnInitialized()
    {
        if (Auth.CurrentUser != null)
        {
            fullName = Auth.CurrentUser.FullName;
            profilePictureUrl = Auth.CurrentUser.ProfilePictureUrl ?? string.Empty;
        }
    }

    protected string GetInitials()
    {
        if (Auth.CurrentUser == null) return "?";
        var parts = Auth.CurrentUser.FullName.Split(' ', StringSplitOptions.RemoveEmptyEntries);
        if (parts.Length >= 2) return $"{parts[0][0]}{parts[1][0]}".ToUpper();
        return Auth.CurrentUser.FullName[0].ToString().ToUpper();
    }

    protected string GetRoleBadgeClass()
    {
        return Auth.CurrentUser?.Role switch
        {
            "Admin" => "bg-red-100 text-red-800",
            "Paid" => "bg-purple-100 text-purple-800",
            _ => "bg-gray-100 text-gray-800"
        };
    }

    protected string GetRoleColorClass()
    {
        return Auth.CurrentUser?.Role switch
        {
            "Admin" => "text-red-600",
            "Paid" => "text-purple-600",
            _ => "text-gray-600"
        };
    }

    protected void HandleSave()
    {
        Auth.UpdateProfile(fullName, string.IsNullOrWhiteSpace(profilePictureUrl) ? null : profilePictureUrl);
        saveMessage = "Profile updated successfully!";
        StateHasChanged();
    }

    protected void UpdateProfilePicture()
    {
        if (!string.IsNullOrWhiteSpace(profilePictureUrl))
        {
            Auth.UpdateProfile(fullName, profilePictureUrl);
            saveMessage = "Profile picture updated!";
            StateHasChanged();
        }
    }

    protected void UpgradeToPaid()
    {
        Auth.UpgradeToRole("Paid");
        saveMessage = "Upgraded to Paid account! 🎉";
        StateHasChanged();
    }

    protected void HandleSignOut()
    {
        Auth.SignOut();
        Nav.NavigateTo("/signin");
    }
}
</pre>
            </div>

            <div class="mt-6 space-y-4">
                <h3 class="text-lg font-semibold text-gray-900">Profile-ready data &amp; auth plumbing</h3>
                <p class="text-xs text-gray-600">If you started the tutorial before this update, replace these files so
                    `/profile` compiles in a fresh Blazor app.</p>

                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto">
                    <div class="text-xs text-gray-500 mb-1">Models/User.cs</div>
                    <pre data-code-title="Models/User.cs"
                        class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">namespace AdminDashboard.Models;

public class User
{
    public int Id { get; set; }
    public string Email { get; set; } = string.Empty;
    public string PasswordHash { get; set; } = string.Empty;
    public string FullName { get; set; } = "New User";
    public string Role { get; set; } = "Free";
    public string? ProfilePictureUrl { get; set; }
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
    public DateTime LastLoginAt { get; set; } = DateTime.UtcNow;
}</pre>
                </div>

                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto">
                    <div class="text-xs text-gray-500 mb-1">Services/IUserAuthService.cs</div>
                    <pre data-code-title="Services/IUserAuthService.cs"
                        class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">using AdminDashboard.Models;

namespace AdminDashboard.Services;

public interface IUserAuthService
{
    User? CurrentUser { get; }
    bool SignUp(string email, string password, string fullName);
    bool SignIn(string email, string password);
    void SignOut();
    void UpdateProfile(string fullName, string? profilePictureUrl);
    void UpgradeToRole(string role);
}</pre>
                </div>

                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto">
                    <div class="text-xs text-gray-500 mb-1">Services/DemoUserAuthService.cs</div>
                    <p class="text-xs text-gray-600 mb-2">Same EF Core service from Steps 3–4, now returning
                        <code>CurrentUser</code> state for the profile page.</p>
                    <pre data-code-title="Services/DemoUserAuthService.cs"
                        class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">using System;
using System.Linq;
using AdminDashboard.Data;
using AdminDashboard.Models;

namespace AdminDashboard.Services;

public class DemoUserAuthService : IUserAuthService
{
    private readonly AppDbContext _db;
    private User? _currentUser;

    public DemoUserAuthService(AppDbContext db)
    {
        _db = db;
    }

    public User? CurrentUser =&gt; _currentUser;

    public bool SignUp(string email, string password, string fullName)
    {
        var normalizedEmail = email.Trim().ToLowerInvariant();

        if (_db.Users.Any(u =&gt; u.Email == normalizedEmail))
        {
            return false;
        }

        var user = new User
        {
            Email = normalizedEmail,
            PasswordHash = password,
            FullName = string.IsNullOrWhiteSpace(fullName) ? "New User" : fullName.Trim(),
            Role = "Free",
            CreatedAt = DateTime.UtcNow,
            LastLoginAt = DateTime.UtcNow
        };

        _db.Users.Add(user);
        _db.SaveChanges();
        _currentUser = user;
        return true;
    }

    public bool SignIn(string email, string password)
    {
        var normalizedEmail = email.Trim().ToLowerInvariant();
        var user = _db.Users.FirstOrDefault(u =&gt; u.Email == normalizedEmail && u.PasswordHash == password);

        if (user == null)
        {
            return false;
        }

        user.LastLoginAt = DateTime.UtcNow;
        _db.SaveChanges();
        _currentUser = user;
        return true;
    }

    public void SignOut() =&gt; _currentUser = null;

    public void UpdateProfile(string fullName, string? profilePictureUrl)
    {
        if (_currentUser == null)
        {
            return;
        }

        var user = _db.Users.FirstOrDefault(u =&gt; u.Id == _currentUser.Id);
        if (user == null)
        {
            return;
        }

        user.FullName = string.IsNullOrWhiteSpace(fullName) ? user.FullName : fullName.Trim();
        user.ProfilePictureUrl = string.IsNullOrWhiteSpace(profilePictureUrl) ? null : profilePictureUrl.Trim();
        _db.SaveChanges();
        _currentUser = user;
    }

    public void UpgradeToRole(string role)
    {
        if (_currentUser == null)
        {
            return;
        }

        var user = _db.Users.FirstOrDefault(u =&gt; u.Id == _currentUser.Id);
        if (user == null)
        {
            return;
        }

        user.Role = role;
        _db.SaveChanges();
        _currentUser = user;
    }
}</pre>
                    <p class="text-[11px] text-gray-600 mt-2">🔐 Hash passwords and use Identity for production apps;
                        plain text storage keeps the tutorial focused.</p>
                </div>

                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto">
                    <div class="text-xs text-gray-500 mb-1">Program.cs (DI registration)</div>
                    <pre data-code-title="Program.cs (snippet)"
                        class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">using AdminDashboard.Services;

builder.Services.AddScoped&lt;IUserAuthService, DemoUserAuthService&gt;();</pre>
                </div>

                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto">
                    <div class="text-xs text-gray-500 mb-1">Add the migration for new fields</div>
                    <pre data-code-title="Terminal"
                        class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">dotnet ef migrations add AddProfileFields
dotnet ef database update</pre>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">📝 Important Notes &amp; Tips</h2>

            <div class="space-y-3">
                <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
                    <h3 class="text-sm font-bold text-yellow-900 mb-2">Update your data + service before pasting</h3>
                    <p class="text-xs sm:text-sm text-yellow-900 mb-3">The profile page expects `CurrentUser`, profile
                        update helpers, and the new columns on `User`. Replace the files using the snippets above and
                        rerun the migration commands or you'll see `CS1061` errors for `CurrentUser`, `UpdateProfile`,
                        and `UpgradeToRole`.</p>
                    <ul class="list-disc ml-5 text-xs sm:text-sm text-yellow-900 space-y-1">
                        <li>Set the `&lt;RootNamespace&gt;` or update namespaces to match your project.</li>
                        <li>Install/restore EF Core tools if `dotnet ef` fails.</li>
                        <li>Re-run `dotnet ef database update` after adding the new migration.</li>
                    </ul>
                </div>

                <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                    <h3 class="text-sm font-bold text-blue-900 mb-2">Profile UX reminders</h3>
                    <ul class="list-disc ml-5 text-xs sm:text-sm text-blue-900 space-y-1">
                        <li>Use the optimistic `saveMessage` banner to confirm actions.</li>
                        <li>The role upgrade button calls `UpgradeToPaid` which just flips the `Role` field for now.
                        </li>
                        <li>Redirect unauthenticated visitors by checking `Auth.CurrentUser` before rendering the
                            layout.</li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">🛠️ How to do it</h2>
            <ol class="list-decimal ml-5 space-y-2 text-gray-700 text-sm sm:text-base">
                <li><strong>Create the page + base:</strong> Add <code>Profile.razor</code>, set
                    <code>@@inherits ProfileBase</code>, and ensure <code>ProfileBase</code> lives in
                    <code>Components/Pages/Demo/DashboardDemo/Profile.razor.cs</code>.</li>
                <li><strong>Redirect unauthenticated users:</strong> Check <code>Auth.CurrentUser</code> at the top of
                    the component and call <code>Nav.NavigateTo("/signin")</code> if null.</li>
                <li><strong>Bind fields to state:</strong> Use <code>fullName</code> and <code>profilePictureUrl</code>
                    fields defined in the base class, and set their initial values in <code>OnInitialized</code>.</li>
                <li><strong>Reuse service helpers:</strong> Hook Save, Update Photo, Upgrade, and Sign Out buttons to
                    <code>HandleSave</code>, <code>UpdateProfilePicture</code>, <code>UpgradeToPaid</code>, and
                    <code>HandleSignOut</code>.</li>
                <li><strong>Show account stats:</strong> Pull <code>CreatedAt</code>, <code>LastLoginAt</code>, and
                    <code>Role</code> straight from <code>Auth.CurrentUser</code> so the UI matches the live demo.</li>
                <li><strong>Give feedback:</strong> Display <code>saveMessage</code> when actions succeed to mirror the
                    optimistic banners in the real page.</li>
            </ol>
        </div>

        <div class="bg-black/3 rounded-2xl p-6 mb-6">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">🎬 Live Demo</h2>
            <p class="text-sm text-gray-600 mb-4">
                The canonical <code>/profile</code> layout learners will see in <code>DashboardDemo</code>:
            </p>
            <div class="bg-white rounded-2xl border border-gray-200 overflow-hidden">
                <div class="max-w-2xl mx-auto p-8">
                    <div class="mb-6 text-center">
                        <h2 class="text-3xl font-bold text-gray-900 mb-2">My Profile</h2>
                        <p class="text-gray-600">Update your account info, avatar, and plan.</p>
                    </div>
                    <div class="grid gap-6 md:grid-cols-2">
                        <div class="bg-gray-50 border border-gray-200 rounded-2xl p-5 text-center">
                            <div
                                class="h-24 w-24 mx-auto rounded-full bg-gray-200 flex items-center justify-center text-2xl font-bold text-gray-500 mb-4">
                                JD</div>
                            <button
                                class="w-full px-4 py-2 bg-blue-600 text-white rounded-lg font-semibold hover:bg-blue-700 transition">Update
                                Photo</button>
                        </div>
                        <div class="space-y-4">
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">Full Name</label>
                                <input type="text" value="Jamie Doe"
                                    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                                <input type="email" value="jamie@demo.com" disabled
                                    class="w-full px-4 py-2 border border-gray-200 bg-gray-50 rounded-lg text-gray-500" />
                            </div>
                            <div class="flex gap-3">
                                <button
                                    class="w-full py-2 bg-white border border-gray-300 text-gray-900 rounded-lg font-semibold hover:bg-green-500 hover:text-white transition">Save
                                    Changes</button>
                                <button
                                    class="w-full py-2 bg-black text-white rounded-lg font-semibold hover:bg-gray-900 transition">Sign
                                    Out</button>
                            </div>
                        </div>
                    </div>
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mt-8">
                        <div class="bg-gray-50 border border-gray-200 rounded-xl p-4 text-center">
                            <p class="text-xs uppercase text-gray-500">Member Since</p>
                            <p class="text-lg font-semibold text-gray-900">Jan 12, 2024</p>
                        </div>
                        <div class="bg-gray-50 border border-gray-200 rounded-xl p-4 text-center">
                            <p class="text-xs uppercase text-gray-500">Account Type</p>
                            <p class="text-lg font-semibold text-purple-600">Paid</p>
                        </div>
                    </div>
                </div>
                <div class="bg-gray-50 border-t border-gray-200 px-6 py-4">
                    <p class="text-xs text-gray-600">
                        💡 <strong>Try it live:</strong> <a href="/profile"
                            class="text-blue-600 hover:text-blue-700 font-medium">Open /profile →</a> after signing in
                        to see the real component.
                    </p>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-xl p-6 border-2 border-green-200 mb-6">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div>
                    <h3 class="text-lg font-semibold text-gray-900 mb-1">Finished Step 5?</h3>
                    <p class="text-sm text-gray-600">Mark this step as complete to track your Admin Dashboard learning
                        progress.</p>
                </div>
                @if (isComplete)
                {
                    <div class="flex items-center gap-3">
                        <span class="px-4 py-2 rounded-full bg-green-100 text-green-700 font-medium">✓ Completed</span>
                        <button @onclick="ResetStep"
                            class="px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors text-sm">Reset</button>
                    </div>
                }
                else
                {
                    <button @onclick="MarkComplete"
                        class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg font-medium transition-colors">
                        Mark as Complete
                    </button>
                }
            </div>
        </div>

        <div class="mt-8">
            <div class="bg-black/90 rounded-2xl p-6 sm:p-8 text-white text-center">
                <p class="text-lg sm:text-xl font-semibold mb-4">Continue Learning</p>
                <p class="text-sm text-gray-300 mb-6 max-w-2xl mx-auto">
                    Auth is locked in. Step 6 moves into analytics so keep the same fidelity while mirroring the demo
                    cards and charts.
                </p>
                <div class="flex flex-col sm:flex-row items-center justify-center gap-3">
                    <a href="/admin-dashboard-guide"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 rounded-xl transition-colors">
                        <span class="text-lg">←</span>
                        <span class="text-sm sm:text-base font-semibold">Back to Guide</span>
                    </a>
                    <a href="/admin-dashboard-examples/step4"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 rounded-xl transition-colors">
                        <span class="text-lg">←</span>
                        <span class="text-sm sm:text-base font-semibold">Previous: Step 4</span>
                    </a>
                    <a href="/admin-dashboard-examples/step6"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700 rounded-xl transition-colors">
                        <span class="text-sm sm:text-base font-semibold">Next: Step 6</span>
                        <span class="text-lg">→</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>