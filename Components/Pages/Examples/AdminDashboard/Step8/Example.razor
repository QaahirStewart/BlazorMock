@page "/admin-dashboard-examples/step8"
@rendermode InteractiveServer
@using BlazorMock.Components.Pages.Examples.AdminDashboard.Step8
@inherits ExampleBase

<PageTitle>Admin Dashboard Step 8: CSS-Only Analytics Polish - Example</PageTitle>

<div class="min-h-screen py-8 rounded-4xl bg-gray-50">
    <div class="w-full mx-auto px-4 max-w-5xl">
        <a href="/admin-dashboard-guide"
            class="inline-flex items-center gap-2 text-sm sm:text-base text-blue-600 hover:text-blue-700 mb-4">
            <span>←</span> Back to Admin Dashboard Guide
        </a>

        <h1 class="text-4xl font-bold text-gray-900 mb-4">
            🎨 Step 8: Make Analytics Look Amazing (Visual Polish)
        </h1>

        <p class="text-xl text-gray-600 mb-8">
            Take the analytics from Step 7 and make them beautiful! We'll add progress bars, gradients, and smooth
            layouts—all with just CSS, no fancy chart libraries needed.
        </p>

        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">What You'll Build</h2>
            <ul class="list-disc ml-6 text-sm sm:text-base text-gray-700 space-y-2 mb-4">
                <li>Visual progress bars showing percentages (using simple CSS width styling)</li>
                <li>Color gradients and rounded corners to make stats pop</li>
                <li>Responsive layouts that look great on both desktop and mobile</li>
                <li>Clean, readable designs that work without complicated chart libraries</li>
            </ul>
            <div class="mt-2 pt-3 border-t border-blue-200">
                <p class="text-xs sm:text-sm text-gray-700 mb-2">
                    <strong>💡 What You'll Use:</strong> CSS styling, Tailwind utilities, inline width binding, responsive design
                </p>
                <div class="flex flex-wrap items-center gap-2">
                    <span class="text-xs sm:text-sm text-gray-600">→ Related tips:</span>
                    <a href="/tips#auth-service"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Auth
                        Service</a>
                    <a href="/tips#tailwind"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Tailwind
                        Layouts</a>
                    <a href="/tips#layout-state"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Layout
                        State</a>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl font-bold text-gray-900 mb-4">🧩 Sample component: reuse the Step 7 sections, add
                CSS-only polish
            </h2>
            <p class="text-sm text-gray-600 mb-4">
                These snippets reuse the exact sections you added in Step 7. You're only layering on Tailwind utility
                classes and inline widths, so keep everything HTML-encoded (double <code>&#64;</code>, <code>&lt;</code>
                entities) when pasting into the docs.
            </p>
            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mb-4">
                <div class="text-xs text-gray-500 mb-1">Paid metric cards with inline width bars</div>
                <p class="text-xs text-gray-600 mb-2">No JS—just Tailwind borders and <code>style="width:…"</code>.</p>
                <pre data-code-title="Analytics.razor — Advanced Analytics"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">&lt;div class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-6 sm:p-8&quot;&gt;
    &lt;div class=&quot;flex items-center justify-between mb-4&quot;&gt;
        &lt;h2 class=&quot;text-xl font-bold text-gray-900&quot;&gt;Advanced Analytics&lt;/h2&gt;
        &lt;span class=&quot;px-2 py-1 text-xs font-medium bg-purple-100 text-purple-800 rounded&quot;&gt;Paid Feature&lt;/span&gt;
    &lt;/div&gt;
    &lt;div class=&quot;grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4&quot;&gt;
        &lt;div class=&quot;border-2 border-gray-200 rounded-xl p-4 hover:border-blue-400 hover:shadow-lg transition-all duration-200&quot;&gt;
            &lt;p class=&quot;text-sm font-medium text-gray-700&quot;&gt;Conversion Rate&lt;/p&gt;
            &lt;p class=&quot;text-2xl font-bold text-gray-900 mt-2&quot;&gt;4.8%&lt;/p&gt;
            &lt;div class=&quot;mt-3 h-2 bg-gray-200 rounded-full overflow-hidden&quot;&gt;
                &lt;div class=&quot;h-full bg-emerald-500&quot; style=&quot;width: 48%&quot;&gt;&lt;/div&gt;
            &lt;/div&gt;
        &lt;/div&gt;
        &lt;div class=&quot;border-2 border-gray-200 rounded-xl p-4 hover:border-blue-400 hover:shadow-lg transition-all duration-200&quot;&gt;
            &lt;p class=&quot;text-sm font-medium text-gray-700&quot;&gt;Revenue per User&lt;/p&gt;
            &lt;p class=&quot;text-2xl font-bold text-gray-900 mt-2&quot;&gt;$24.50&lt;/p&gt;
            &lt;p class=&quot;text-xs text-emerald-600 mt-1&quot;&gt;↑ $2.30 vs last month&lt;/p&gt;
        &lt;/div&gt;
        &lt;div class=&quot;border-2 border-gray-200 rounded-xl p-4 hover:border-blue-400 hover:shadow-lg transition-all duration-200&quot;&gt;
            &lt;p class=&quot;text-sm font-medium text-gray-700&quot;&gt;Customer Lifetime Value&lt;/p&gt;
            &lt;p class=&quot;text-2xl font-bold text-gray-900 mt-2&quot;&gt;$892&lt;/p&gt;
            &lt;p class=&quot;text-xs text-blue-600 mt-1&quot;&gt;↑ 18% growth&lt;/p&gt;
        &lt;/div&gt;
    &lt;/div&gt;
&lt;/div&gt;</pre>
                <p class="text-[11px] text-gray-600 mt-2">Keep this block in the same place as Step 7; you're just
                    ensuring each tile has hover borders, inline width bars, and textual deltas.</p>
            </div>

            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto">
                <div class="text-xs text-gray-500 mb-1">Traffic sources list with responsive bars</div>
                <p class="text-xs text-gray-600 mb-2">Same list drives both the desktop rows and mobile cards.</p>
                <pre data-code-title="Analytics.razor — Traffic Sources"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">&lt;div class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-6 sm:p-8&quot;&gt;
    &lt;h2 class=&quot;text-xl font-bold text-gray-900 mb-4&quot;&gt;Traffic Sources&lt;/h2&gt;
    &lt;div class=&quot;hidden sm:block space-y-0 divide-y divide-gray-200&quot;&gt;
        @@foreach (var source in trafficSources)
        {
            &lt;div class=&quot;flex items-center justify-between py-4 first:pt-0 last:pb-0&quot;&gt;
                &lt;div class=&quot;flex items-center gap-3&quot;&gt;
                    &lt;span class=&quot;text-2xl&quot;&gt;@@source.Icon&lt;/span&gt;
                    &lt;div&gt;
                        &lt;p class=&quot;text-sm font-medium text-gray-900&quot;&gt;@@source.Name&lt;/p&gt;
                        &lt;p class=&quot;text-xs text-gray-500&quot;&gt;@@source.Visitors visitors&lt;/p&gt;
                    &lt;/div&gt;
                &lt;/div&gt;
                &lt;div class=&quot;text-right&quot;&gt;
                    &lt;p class=&quot;text-sm font-semibold text-gray-900&quot;&gt;@@source.Percentage%&lt;/p&gt;
                    &lt;div class=&quot;w-24 h-2 bg-gray-200 rounded-full overflow-hidden mt-1&quot;&gt;
                        &lt;div class=&quot;h-full bg-blue-500&quot; style=&quot;width: @@(source.Percentage)%&quot;&gt;&lt;/div&gt;
                    &lt;/div&gt;
                &lt;/div&gt;
            &lt;/div&gt;
        }
    &lt;/div&gt;
    &lt;div class=&quot;grid grid-cols-1 gap-3 sm:hidden&quot;&gt;
        @@foreach (var source in trafficSources)
        {
            &lt;div class=&quot;border-2 border-gray-200 rounded-xl p-4 hover:border-gray-300 hover:shadow-lg transition-all duration-200&quot;&gt;
                &lt;div class=&quot;flex items-center gap-3 mb-3&quot;&gt;
                    &lt;span class=&quot;text-2xl&quot;&gt;@@source.Icon&lt;/span&gt;
                    &lt;div class=&quot;flex-1&quot;&gt;
                        &lt;p class=&quot;text-sm font-medium text-gray-900&quot;&gt;@@source.Name&lt;/p&gt;
                        &lt;p class=&quot;text-xs text-gray-500&quot;&gt;@@source.Visitors visitors&lt;/p&gt;
                    &lt;/div&gt;
                    &lt;p class=&quot;text-sm font-semibold text-gray-900&quot;&gt;@@source.Percentage%&lt;/p&gt;
                &lt;/div&gt;
                &lt;div class=&quot;w-full h-2 bg-gray-200 rounded-full overflow-hidden&quot;&gt;
                    &lt;div class=&quot;h-full bg-blue-500&quot; style=&quot;width: @@(source.Percentage)%&quot;&gt;&lt;/div&gt;
                &lt;/div&gt;
            &lt;/div&gt;
        }
    &lt;/div&gt;
&lt;/div&gt;</pre>
                <p class="text-[11px] text-gray-600 mt-2">Nothing new to wire up—this is the same
                    <code>trafficSources</code> sequence from Step 7. Just keep the dual containers (`hidden sm:block`
                    vs `sm:hidden`) so desktop rows and mobile cards stay in sync.</p>
            </div>

            <p class="text-xs text-gray-600 mt-4">
                Notice that everything here is deterministic. The numbers are hard-coded on purpose so documentation,
                marketing screenshots, and the live demo stay in sync.
            </p>
        </div>

        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">📝 Important Notes & Tips</h2>
            <div class="space-y-3">
                <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                    <h3 class="text-sm font-bold text-blue-900 mb-3">💡 Pro Tips</h3>
                    <div class="space-y-4">
                        <div>
                            <p class="text-sm font-semibold text-blue-900 mb-2">Pair bars with labels</p>
                            <p class="text-xs text-blue-800">Every bar in the demo has a headline, value, and helper
                                copy so users never have to interpret color alone.</p>
                        </div>
                        <div>
                            <p class="text-sm font-semibold text-blue-900 mb-2">Use inline styles sparingly</p>
                            <p class="text-xs text-blue-800">Inline <code>style="width:…"</code> is perfect for quick
                                bars, but keep everything else in Tailwind utility classes for consistency.</p>
                        </div>
                        <div>
                            <p class="text-sm font-semibold text-blue-900 mb-2">Keep the gradient CTA</p>
                            <p class="text-xs text-blue-800">The Free branch from Step 7 already has the purple gradient
                                CTA—leave it in place so the polish reinforces the upgrade story.</p>
                        </div>
                        <div>
                            <p class="text-sm font-semibold text-blue-900 mb-2">Stay deterministic</p>
                            <p class="text-xs text-blue-800">The traffic source list lives in <code>AnalyticsBase</code>
                                so the page never surprises you with random datasets mid-tutorial.</p>
                        </div>
                    </div>
                </div>
                <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
                    <h3 class="text-sm font-bold text-yellow-900 mb-2">⚠️ Troubleshooting</h3>
                    <ul class="list-disc ml-6 text-sm text-yellow-900 space-y-1">
                        <li>Bars not visible? Confirm your inline width strings include the percent sign (e.g.,
                            <code>style="width: 48%"</code>).
                        </li>
                        <li>Traffic percentages overflow? Keep <code>w-24</code> (desktop) and <code>w-full</code>
                            (mobile) wrappers so bars stay inside rounded capsules.</li>
                        <li>Colors feel off-brand? Use Tailwind's extended palette (<code>bg-emerald-500</code>,
                            <code>bg-blue-500</code>) to stay consistent with the rest of the dashboard.
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-xl p-6 mb-6">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">🛠️ How to do it</h2>
            <ol class="list-decimal ml-5 space-y-2 text-gray-700 text-sm sm:text-base">
                <li><strong>Start from Step 7:</strong> Keep the same markup blocks (Advanced Analytics, Traffic
                    Sources, Free CTA) and adjust only classes/inline styles.</li>
                <li><strong>Hard-code the visual targets:</strong> The canonical demo intentionally uses static values
                    (4.8%, 48%) to focus on layout, not data pipelines.</li>
                <li><strong>Bind width as literal percent:</strong> Output
                    <code>style="width: @@(source.Percentage)%"</code> inside fixed-width wrappers so bars never
                    overflow.</li>
                <li><strong>Mirror responsive containers:</strong> Desktop rows live in a <code>hidden sm:block</code>
                    div while mobile cards render under <code>sm:hidden</code> using the same data.</li>
                <li><strong>Keep the gradient CTA:</strong> When <code>IsPaidOrAdmin</code> is false, leave the purple
                    gradient block intact so Free users see a polished upgrade path.</li>
            </ol>
        </div>

        <div class="bg-black/3 rounded-2xl p-6 mb-6">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">🎬 Live Demo</h2>
            <p class="text-sm text-gray-600 mb-4">
                Visit <code>/analytics</code> with both Free and Paid accounts to see the CSS-only visuals fade in/out
                based on plan. Admin users also get the red summary grid from Step 7.
            </p>
            <div class="bg-white rounded-2xl border border-gray-200 overflow-hidden">
                <div class="max-w-3xl mx-auto p-8 space-y-6">
                    <div class="text-center">
                        <h2 class="text-3xl font-bold text-gray-900 mb-2">Advanced Analytics</h2>
                        <p class="text-gray-600">Inline bars + badges deliver instant visual cues without any chart
                            libraries.</p>
                    </div>
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
                        <div class="border border-gray-200 rounded-2xl p-5 text-center">
                            <p class="text-xs uppercase text-gray-500 mb-1">Conversion Rate</p>
                            <p class="text-2xl font-semibold text-gray-900">4.8%</p>
                            <div class="mt-3 h-2 bg-gray-200 rounded-full overflow-hidden">
                                <div class="h-full bg-emerald-500" style="width: 48%"></div>
                            </div>
                        </div>
                        <div class="border border-gray-200 rounded-2xl p-5 text-center">
                            <p class="text-xs uppercase text-gray-500 mb-1">Revenue / User</p>
                            <p class="text-2xl font-semibold text-gray-900">$24.50</p>
                            <p class="text-xs text-emerald-600 mt-1">↑ $2.30 vs last month</p>
                        </div>
                        <div class="border border-gray-200 rounded-2xl p-5 text-center">
                            <p class="text-xs uppercase text-gray-500 mb-1">CLTV</p>
                            <p class="text-2xl font-semibold text-gray-900">$892</p>
                            <p class="text-xs text-blue-600 mt-1">↑ 18% growth</p>
                        </div>
                    </div>
                    <div class="bg-gray-50 border border-gray-200 rounded-2xl p-6">
                        <h3 class="text-lg font-semibold text-gray-900 mb-4">Traffic Sources</h3>
                        <div class="space-y-3">
                            <div class="flex items-center justify-between">
                                <div class="flex items-center gap-3">
                                    <span class="text-2xl">🔍</span>
                                    <div>
                                        <p class="text-sm font-medium text-gray-900">Google Search</p>
                                        <p class="text-xs text-gray-500">1,234 visitors</p>
                                    </div>
                                </div>
                                <div class="text-right">
                                    <p class="text-sm font-semibold text-gray-900">42%</p>
                                    <div class="w-24 h-2 bg-gray-200 rounded-full overflow-hidden mt-1">
                                        <div class="h-full bg-blue-500" style="width: 42%"></div>
                                    </div>
                                </div>
                            </div>
                            <div class="flex items-center justify-between">
                                <div class="flex items-center gap-3">
                                    <span class="text-2xl">📱</span>
                                    <div>
                                        <p class="text-sm font-medium text-gray-900">Social Media</p>
                                        <p class="text-xs text-gray-500">892 visitors</p>
                                    </div>
                                </div>
                                <div class="text-right">
                                    <p class="text-sm font-semibold text-gray-900">30%</p>
                                    <div class="w-24 h-2 bg-gray-200 rounded-full overflow-hidden mt-1">
                                        <div class="h-full bg-blue-500" style="width: 30%"></div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="bg-gray-50 border-t border-gray-200 px-6 py-4">
                    <p class="text-xs text-gray-600">
                        💡 <strong>Reminder:</strong> CSS-only visuals keep the tutorial approachable and render exactly
                        the same in docs, videos, and the running Blazor app.
                    </p>
                </div>
            </div>
        </div>

        <!-- Mark Complete Section -->
        <div class="bg-white rounded-xl p-6 border-2 border-green-200 mb-6">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div>
                    <h3 class="text-lg font-semibold text-gray-900 mb-1">Finished Step 8?</h3>
                    <p class="text-sm text-gray-600">Mark this step as complete to track your Admin Dashboard learning
                        progress.</p>
                </div>
                @if (isComplete)
                {
                    <div class="flex items-center gap-3">
                        <span class="px-4 py-2 rounded-full bg-green-100 text-green-700 font-medium">✓ Completed</span>
                        <button @onclick="ResetStep"
                            class="px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors text-sm">
                            Reset
                        </button>
                    </div>
                }
                else
                {
                    <button @onclick="MarkComplete"
                        class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg font-medium transition-colors">
                        Mark as Complete
                    </button>
                }
            </div>
        </div>

        <!-- Navigation -->
        <div class="mt-8">
            <div class="bg-black/90 rounded-2xl p-6 sm:p-8 text-white text-center">
                <p class="text-lg sm:text-xl font-semibold mb-4">Next up: Dashboard Shell</p>
                <p class="text-sm text-gray-300 mb-6 max-w-2xl mx-auto">
                    With analytics visuals in place, you're ready to create the main <code>/admin/dashboard</code> shell
                    that surfaces key metrics, navigation, and quick actions for admins.
                </p>
                <div class="flex flex-col sm:flex-row items-center justify-center gap-3">
                    <a href="/admin-dashboard-guide"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 rounded-xl transition-colors">
                        <span class="text-lg">←</span>
                        <span class="text-sm sm:text-base font-semibold">Back to Guide</span>
                    </a>
                    <a href="/admin-dashboard-examples/step7"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 rounded-xl transition-colors">
                        <span class="text-lg">←</span>
                        <span class="text-sm sm:text-base font-semibold">Previous: Step 7</span>
                    </a>
                    <a href="/admin-dashboard-examples/step9"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700 rounded-xl transition-colors">
                        <span class="text-sm sm:text-base font-semibold">Next: Step 9</span>
                        <span class="text-lg">→</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>