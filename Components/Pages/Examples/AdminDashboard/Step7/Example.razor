@page "/admin-dashboard-examples/step7"
@rendermode InteractiveServer
@using BlazorMock.Components.Pages.Examples.AdminDashboard.Step7
@inherits ExampleBase

<PageTitle>Admin Dashboard Step 7: Paid Analytics Sections - Example</PageTitle>

<div class="min-h-screen py-8 rounded-4xl bg-gray-50">
    <div class="w-full mx-auto px-4 max-w-5xl">
        <a href="/admin-dashboard-guide"
            class="inline-flex items-center gap-2 text-sm sm:text-base text-blue-600 hover:text-blue-700 mb-4">
            <span>←</span> Back to Admin Dashboard Guide
        </a>

        <h1 class="text-4xl font-bold text-gray-900 mb-4">
            📊 Step 7: Paid analytics gating + traffic sources
        </h1>

        <p class="text-xl text-gray-600 mb-8">
            Mirror the paid-only sections of <code>/analytics</code> so learners see the same upgrade CTA, traffic source list, and admin summary grid that ship in <code>DashboardDemo</code>—all powered by <code>IUserAuthService</code>, not EF queries.
        </p>

        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">What You'll Learn</h2>
            <ul class="list-disc ml-6 text-sm sm:text-base text-gray-700 space-y-2 mb-4">
                <li>Use <code>AnalyticsBase</code> helpers (<code>IsPaidOrAdmin</code>/<code>IsAdmin</code>) to flip UI states.</li>
                <li>Display the curated traffic source list on desktop rows + mobile cards.</li>
                <li>Show a locked upgrade CTA for Free users that deep-links to <code>/profile</code>.</li>
                <li>Reuse <code>Auth.AllUsers</code> to surface admin-only totals with zero EF work.</li>
            </ul>
            <div class="mt-2 pt-3 border-t border-blue-200">
                <p class="text-xs sm:text-sm text-gray-700 mb-2">
                    <strong>💡 Key Concepts:</strong> Service-driven feature gating, deterministic sample data, accessibility-friendly locked states.
                </p>
                <div class="flex flex-wrap items-center gap-2">
                    <span class="text-xs sm:text-sm text-gray-600">→ Related tips:</span>
                    <a href="/tips#auth-service"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Auth Service</a>
                    <a href="/tips#layout-state"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Layout State</a>
                    <a href="/tips#tailwind"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Tailwind Layouts</a>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl font-bold text-gray-900 mb-4">🧩 Sample component: paid + admin sections</h2>
            <p class="text-sm text-gray-600 mb-4">
                These excerpts are lifted directly from <code>Components/Pages/Demo/DashboardDemo/Analytics.razor</code>. Keep them HTML-encoded (notice every <code>&#64;</code> is written as <code>&#64;&#64;</code>) so the docs render safely.
            </p>
            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mb-4">
                <div class="text-xs text-gray-500 mb-1">Analytics.razor — Paid vs Free toggle</div>
                <p class="text-xs text-gray-600 mb-2">When <code>IsPaidOrAdmin</code> is false, show the upgrade CTA instead of traffic cards.</p>
                <pre data-code-title="Paid analytics section"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">@@if (IsPaidOrAdmin)
{
    &lt;div class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-6 sm:p-8&quot;&gt;
        &lt;div class=&quot;flex items-center justify-between mb-4&quot;&gt;
            &lt;h2 class=&quot;text-xl font-bold text-gray-900&quot;&gt;Advanced Analytics&lt;/h2&gt;
            &lt;span class=&quot;px-2 py-1 text-xs font-medium bg-purple-100 text-purple-800 rounded&quot;&gt;Paid Feature&lt;/span&gt;
        &lt;/div&gt;
        &lt;!-- metric cards trimmed for brevity --&gt;
    &lt;/div&gt;

    &lt;div class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-6 sm:p-8&quot;&gt;
        &lt;h2 class=&quot;text-xl font-bold text-gray-900 mb-4&quot;&gt;Traffic Sources&lt;/h2&gt;
        &lt;div class=&quot;hidden sm:block divide-y divide-gray-200&quot;&gt;
            @@foreach (var source in trafficSources)
            {
                &lt;div class=&quot;flex items-center justify-between py-4&quot;&gt;
                    &lt;div class=&quot;flex items-center gap-3&quot;&gt;
                        &lt;span class=&quot;text-2xl&quot;&gt;@@source.Icon&lt;/span&gt;
                        &lt;div&gt;
                            &lt;p class=&quot;text-sm font-medium text-gray-900&quot;&gt;@@source.Name&lt;/p&gt;
                            &lt;p class=&quot;text-xs text-gray-500&quot;&gt;@@source.Visitors visitors&lt;/p&gt;
                        &lt;/div&gt;
                    &lt;/div&gt;
                    &lt;div class=&quot;text-right&quot;&gt;
                        &lt;p class=&quot;text-sm font-semibold text-gray-900&quot;&gt;@@source.Percentage%&lt;/p&gt;
                        &lt;div class=&quot;w-24 h-2 bg-gray-200 rounded-full overflow-hidden mt-1&quot;&gt;
                            &lt;div class=&quot;h-full bg-blue-500&quot; style=&quot;width: @@(source.Percentage)%&quot;&gt;&lt;/div&gt;
                        &lt;/div&gt;
                    &lt;/div&gt;
                &lt;/div&gt;
            }
        &lt;/div&gt;
        &lt;div class=&quot;grid grid-cols-1 gap-3 sm:hidden&quot;&gt;
            @@foreach (var source in trafficSources)
            {
                &lt;!-- mobile card markup --&gt;
            }
        &lt;/div&gt;
    &lt;/div&gt;
}
else
{
    &lt;div class=&quot;bg-gradient-to-br from-purple-50 to-indigo-50 rounded-2xl border-2 border-purple-200 p-8 text-center&quot;&gt;
        &lt;h3 class=&quot;text-2xl font-bold text-gray-900 mb-2&quot;&gt;Unlock Advanced Analytics&lt;/h3&gt;
        &lt;p class=&quot;text-gray-600 mb-6&quot;&gt;Upgrade to a Paid account to access conversion tracking, revenue metrics, traffic sources, and more.&lt;/p&gt;
        &lt;div class=&quot;flex flex-wrap justify-center gap-3&quot;&gt;
            &lt;a href=&quot;/profile&quot; class=&quot;px-6 py-3 bg-purple-600 hover:bg-purple-700 text-white font-semibold rounded-lg&quot;&gt;Upgrade Now&lt;/a&gt;
            &lt;button class=&quot;px-6 py-3 bg-white border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50&quot;&gt;Learn More&lt;/button&gt;
        &lt;/div&gt;
    &lt;/div&gt;
}</pre>
            </div>

            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto">
                <div class="text-xs text-gray-500 mb-1">Analytics.razor — Admin summary</div>
                <p class="text-xs text-gray-600 mb-2">Admins get an extra grid fueled entirely by <code>Auth.AllUsers</code>.</p>
                <pre data-code-title="Admin-only metrics"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">@@if (IsAdmin)
{
    &lt;div class=&quot;bg-gradient-to-br from-red-50 to-orange-50 rounded-2xl border-2 border-red-200 p-6 sm:p-8&quot;&gt;
        &lt;div class=&quot;flex items-center gap-2 mb-4&quot;&gt;
            &lt;h2 class=&quot;text-xl font-bold text-red-900&quot;&gt;Admin Analytics&lt;/h2&gt;
            &lt;span class=&quot;px-2 py-1 text-xs font-medium bg-red-100 text-red-800 rounded&quot;&gt;Admin Only&lt;/span&gt;
        &lt;/div&gt;
        &lt;div class=&quot;grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4&quot;&gt;
            &lt;div class=&quot;bg-white rounded-lg p-4 border-2 border-red-200&quot;&gt;
                &lt;p class=&quot;text-xs font-medium text-red-600 uppercase&quot;&gt;Total Users&lt;/p&gt;
                &lt;p class=&quot;text-2xl font-bold text-gray-900 mt-1&quot;&gt;@@Auth.AllUsers.Count&lt;/p&gt;
            &lt;/div&gt;
            &lt;div class=&quot;bg-white rounded-lg p-4 border-2 border-red-200&quot;&gt;
                &lt;p class=&quot;text-xs font-medium text-red-600 uppercase&quot;&gt;Free Accounts&lt;/p&gt;
                &lt;p class=&quot;text-2xl font-bold text-gray-900 mt-1&quot;&gt;@@Auth.AllUsers.Count(u =&gt; u.Role == &quot;Free&quot;)&lt;/p&gt;
            &lt;/div&gt;
            &lt;div class=&quot;bg-white rounded-lg p-4 border-2 border-red-200&quot;&gt;
                &lt;p class=&quot;text-xs font-medium text-red-600 uppercase&quot;&gt;Paid Accounts&lt;/p&gt;
                &lt;p class=&quot;text-2xl font-bold text-gray-900 mt-1&quot;&gt;@@Auth.AllUsers.Count(u =&gt; u.Role == &quot;Paid&quot;)&lt;/p&gt;
            &lt;/div&gt;
            &lt;div class=&quot;bg-white rounded-lg p-4 border-2 border-red-200&quot;&gt;
                &lt;p class=&quot;text-xs font-medium text-red-600 uppercase&quot;&gt;Admins&lt;/p&gt;
                &lt;p class=&quot;text-2xl font-bold text-gray-900 mt-1&quot;&gt;@@Auth.AllUsers.Count(u =&gt; u.Role == &quot;Admin&quot;)&lt;/p&gt;
            &lt;/div&gt;
        &lt;/div&gt;
    &lt;/div&gt;
}</pre>
            </div>

            <p class="text-xs text-gray-600 mt-4">
                Important: the real demo keeps these cards static because the focus is on teaching Blazor layouts, not on wiring live analytics services. Stick to the shared <code>trafficSources</code> list and <code>IUserAuthService</code> data so documentation screenshots always match the running app.
            </p>

            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mt-6">
                <div class="text-xs text-gray-500 mb-1">Analytics.razor.cs — helper state</div>
                <p class="text-xs text-gray-600 mb-2">All gating logic lives in the code-behind—no EF, no extra services.</p>
                <pre data-code-title="AnalyticsBase helpers"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">public partial class AnalyticsBase : ComponentBase
{
    [Inject] protected IUserAuthService Auth { get; set; } = default!;
    [Inject] protected NavigationManager Nav { get; set; } = default!;

    protected bool IsPaidOrAdmin => Auth.CurrentUser?.Role == &quot;Paid&quot; || Auth.CurrentUser?.Role == &quot;Admin&quot;;
    protected bool IsAdmin => Auth.CurrentUser?.Role == &quot;Admin&quot;;

    protected List&lt;TrafficSource&gt; trafficSources = new()
    {
        new TrafficSource { Icon = &quot;🔍&quot;, Name = &quot;Google Search&quot;, Visitors = 1234, Percentage = 42 },
        new TrafficSource { Icon = &quot;📱&quot;, Name = &quot;Social Media&quot;, Visitors = 892, Percentage = 30 },
        new TrafficSource { Icon = &quot;📧&quot;, Name = &quot;Email Campaign&quot;, Visitors = 567, Percentage = 19 },
        new TrafficSource { Icon = &quot;🔗&quot;, Name = &quot;Direct&quot;, Visitors = 234, Percentage = 9 }
    };

    protected class TrafficSource
    {
        public string Icon { get; set; } = string.Empty;
        public string Name { get; set; } = string.Empty;
        public int Visitors { get; set; }
        public int Percentage { get; set; }
    }
}</pre>
            </div>
        </div>

        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">🛠️ How to do it</h2>
            <ol class="list-decimal ml-5 space-y-2 text-gray-700 text-sm sm:text-base">
                <li><strong>Inherit <code>AnalyticsBase</code>:</strong> Keep markup lean by putting gating booleans + traffic data in the partial class just like the demo.</li>
                <li><strong>Branch on plan:</strong> Wrap premium cards + traffic list in <code>&#64;&#64;if (IsPaidOrAdmin)</code> and always supply a helpful locked state for Free accounts.</li>
                <li><strong>Reuse sample data:</strong> Traffic rows come from a simple list so the docs never drift. Resist adding random datasets per tutorial.</li>
                <li><strong>Surface admin context:</strong> Use <code>Auth.AllUsers</code> for totals instead of duplicating EF queries—admins already loaded that data when they signed in.</li>
                <li><strong>Link upgrades to /profile:</strong> Send users to the existing plan upgrade flow so the experience stays consistent across steps.</li>
            </ol>
        </div>

        <div class="bg-black/3 rounded-2xl p-6 mb-6">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">🎬 Live Demo</h2>
            <p class="text-sm text-gray-600 mb-4">
                Toggle between Free and Paid demo accounts on <code>/signin</code> to watch the analytics view switch between the locked CTA, premium charts, and the admin summary grid.
            </p>
            <div class="bg-white rounded-2xl border border-gray-200 overflow-hidden">
                <div class="max-w-3xl mx-auto p-8 space-y-6">
                    <div class="text-center">
                        <h2 class="text-3xl font-bold text-gray-900 mb-2">Advanced Analytics</h2>
                        <p class="text-gray-600">Paid plans see conversion rate, revenue/user, CLTV, and traffic sources at a glance.</p>
                    </div>
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                        <div class="bg-white border border-gray-200 rounded-2xl p-5 text-center">
                            <p class="text-xs uppercase text-gray-500">Conversion Rate</p>
                            <p class="text-2xl font-semibold text-gray-900">4.8%</p>
                            <div class="mt-3 h-2 bg-gray-200 rounded-full overflow-hidden">
                                <div class="h-full bg-emerald-500" style="width: 48%"></div>
                            </div>
                        </div>
                        <div class="bg-white border border-gray-200 rounded-2xl p-5 text-center">
                            <p class="text-xs uppercase text-gray-500">Revenue / User</p>
                            <p class="text-2xl font-semibold text-gray-900">$24.50</p>
                            <p class="text-xs text-emerald-600 mt-1">↑ $2.30 vs last month</p>
                        </div>
                    </div>
                    <div class="bg-purple-50 border border-purple-100 rounded-2xl p-6 text-center">
                        <h3 class="text-xl font-bold text-gray-900 mb-2">Free plan view</h3>
                        <p class="text-sm text-gray-600 mb-4">Free users get a friendly upgrade panel that links over to <code>/profile</code> for plan changes.</p>
                        <div class="flex flex-col sm:flex-row gap-3 justify-center">
                            <a href="/profile" class="px-6 py-3 bg-purple-600 text-white rounded-lg font-semibold hover:bg-purple-700 transition">Upgrade Now</a>
                            <button class="px-6 py-3 bg-white border border-gray-300 text-gray-700 rounded-lg font-semibold hover:bg-gray-50 transition">Learn More</button>
                        </div>
                    </div>
                </div>
                <div class="bg-gray-50 border-t border-gray-200 px-6 py-4">
                    <p class="text-xs text-gray-600">
                        💡 <strong>Reminder:</strong> These cards are intentionally hard-coded so the guide stays screenshot-stable. When you need real analytics, layer them on in a separate tutorial.
                    </p>
                </div>
            </div>
        </div>

        <!-- Mark Complete Section -->
        <div class="bg-white rounded-xl p-6 border-2 border-green-200 mb-6">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div>
                    <h3 class="text-lg font-semibold text-gray-900 mb-1">Finished Step 7?</h3>
                    <p class="text-sm text-gray-600">Mark this step as complete to track your Admin Dashboard learning
                        progress.</p>
                </div>
                @if (isComplete)
                {
                    <div class="flex items-center gap-3">
                        <span class="px-4 py-2 rounded-full bg-green-100 text-green-700 font-medium">✓ Completed</span>
                        <button @onclick="ResetStep"
                            class="px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors text-sm">
                            Reset
                        </button>
                    </div>
                }
                else
                {
                    <button @onclick="MarkComplete"
                        class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg font-medium transition-colors">
                        Mark as Complete
                    </button>
                }
            </div>
        </div>

        <!-- Navigation -->
        <div class="mt-8">
            <div class="bg-black/90 rounded-2xl p-6 sm:p-8 text-white text-center">
                <p class="text-lg sm:text-xl font-semibold mb-4">Continue Learning</p>
                <p class="text-sm text-gray-300 mb-6 max-w-2xl mx-auto">
                    With your analytics page displaying metrics and activity, you're ready to add visual elements like
                    bars and heatmaps in Step 8.
                </p>
                <div class="flex flex-col sm:flex-row items-center justify-center gap-3">
                    <a href="/admin-dashboard-guide"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 rounded-xl transition-colors">
                        <span class="text-lg">←</span>
                        <span class="text-sm sm:text-base font-semibold">Back to Guide</span>
                    </a>
                    <a href="/admin-dashboard-examples/step6"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 rounded-xl transition-colors">
                        <span class="text-lg">←</span>
                        <span class="text-sm sm:text-base font-semibold">Previous: Step 6</span>
                    </a>
                    <a href="/admin-dashboard-examples/step8"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700 rounded-xl transition-colors">
                        <span class="text-sm sm:text-base font-semibold">Next: Step 8</span>
                        <span class="text-lg">→</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>