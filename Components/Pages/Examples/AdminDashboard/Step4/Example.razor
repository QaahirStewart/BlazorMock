@page "/admin-dashboard-examples/step4"
@rendermode InteractiveServer
@inherits BlazorMock.Components.Pages.Examples.AdminDashboard.Step4.ExampleBase

<PageTitle>Admin Dashboard Step 4: Sign In Page - Example</PageTitle>

<div class="min-h-screen py-8 rounded-4xl bg-gray-50">
    <div class="w-full mx-auto px-4 max-w-5xl">
        <!-- Back link -->
        <a href="/admin-dashboard-guide"
            class="inline-flex items-center gap-2 text-sm sm:text-base text-blue-600 hover:text-blue-700 mb-4">
            <span>←</span> Back to Admin Dashboard Guide
        </a>

        <!-- Title -->
        <h1 class="text-4xl font-bold text-gray-900 mb-4">
            🔐 Step 4: Build the /signin page (SignIn.razor)
        </h1>

        <!-- Description -->
        <p class="text-xl text-gray-600 mb-8">
            Recreate the exact <code>/signin</code> page that ships with the Admin Dashboard demo so returning users get the real two-column layout, passkey CTA, and auto-fill demo accounts they see in <code>DashboardDemo</code>.
        </p>

        <!-- What You'll Learn Section -->
        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">What You'll Learn</h2>

            <ul class="list-disc ml-6 text-sm sm:text-base text-gray-700 space-y-2 mb-4">
                <li>Mirror the polished two-column layout from <code>DashboardDemo/Auth/SignIn.razor</code>.</li>
                <li>Bind inputs directly to component fields with <code>@@bind</code> and show inline errors.</li>
                <li>Call the shared <code>IUserAuthService</code> from <code>SignInBase</code> instead of touching EF or <code>AppDbContext</code> in the page.</li>
                <li>Add the "Sign in with Passkey" button that simulates FIDO-style auth.</li>
                <li>Use clickable demo account cards to auto-fill credentials via <code>SetDemoUser</code>.</li>
            </ul>

            <div class="mt-2 pt-3 border-t border-blue-200">
                <p class="text-xs sm:text-sm text-gray-700 mb-2">
                    <strong>💡 Key Concepts:</strong> Two-way binding, calling shared services, optimistic navigation, and demo-friendly UX chips.
                </p>
                <div class="flex flex-wrap items-center gap-2">
                    <span class="text-xs sm:text-sm text-gray-600">→ Related tips:</span>
                    <a href="/tips#data-annotations"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Data Annotations</a>
                    <a href="/tips#editform-+-validation"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">EditForm + Validation</a>
                    <a href="/tips#auth-service"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Auth Service</a>
                </div>
            </div>
        </div>

        <!-- Sign In Form Example -->
        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl font-bold text-gray-900 mb-4">🧩 Sample Component: SignIn.razor (/signin)</h2>

            <p class="text-sm text-gray-600 mb-4">
                Copy the real <code>SignIn.razor</code> file from the demo to guarantee your tutorial matches what learners see live.
            </p>

            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mb-4">
                <div class="text-xs text-gray-500 mb-1">Components/Pages/Demo/DashboardDemo/Auth/SignIn.razor</div>
                <p class="text-xs text-gray-600 mb-2">Blazor sign in page with passkey CTA, inline errors, and demo cards wired to <code>SignInBase</code>.</p>
                <ul class="list-disc ml-4 mb-3 text-[11px] text-gray-600 space-y-1">
                    <li>Uses simple inputs with <code>@@bind</code> plus manual validation messages.</li>
                    <li>Calls <code>HandleSignIn</code> / <code>HandlePasskeySignIn</code> implemented in the shared base class.</li>
                    <li>Includes the clickable demo account list hooked to <code>SetDemoUser</code>.</li>
                </ul>
                <pre data-code-title="Components/Pages/Demo/DashboardDemo/Auth/SignIn.razor"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">@@page "/signin"
@@rendermode InteractiveServer
@@using AdminDashboard.Components.Pages.Auth
@@inherits SignInBase

&lt;PageTitle&gt;Sign In&lt;/PageTitle&gt;

&lt;div class=&quot;py-10 px-4 rounded-3xl bg-gray-50&quot;&gt;
    &lt;div class=&quot;max-w-6xl mx-auto px-4&quot;&gt;
        &lt;div class=&quot;grid grid-cols-1 lg:grid-cols-2 gap-6 items-start&quot;&gt;
            &lt;!-- Left: Sign In Form --&gt;
            &lt;div class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-6 sm:p-8&quot;&gt;
                &lt;div class=&quot;text-center mb-6&quot;&gt;
                    &lt;div class=&quot;h-16 w-16 bg-blue-600 rounded-full flex items-center justify-center mx-auto mb-4&quot;&gt;
                        &lt;svg class=&quot;w-8 h-8 text-white&quot; fill=&quot;none&quot; stroke=&quot;currentColor&quot; viewBox=&quot;0 0 24 24&quot;&gt;
                            &lt;path stroke-linecap=&quot;round&quot; stroke-linejoin=&quot;round&quot; stroke-width=&quot;2&quot;
                                d=&quot;M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z&quot; /&gt;
                        &lt;/svg&gt;
                    &lt;/div&gt;
                    &lt;h1 class=&quot;text-3xl font-bold text-gray-900&quot;&gt;Welcome Back&lt;/h1&gt;
                    &lt;p class=&quot;text-gray-600 mt-2&quot;&gt;Sign in to your account&lt;/p&gt;
                &lt;/div&gt;

                @@if (!string.IsNullOrEmpty(errorMessage))
                {
                    &lt;div class=&quot;mb-4 p-3 bg-red-50 border border-red-200 rounded-lg text-red-700 text-sm&quot;&gt;
                        @@errorMessage
                    &lt;/div&gt;
                }

                &lt;div class=&quot;space-y-4&quot;&gt;
                    &lt;div&gt;
                        &lt;label class=&quot;block text-sm font-medium text-gray-700 mb-2&quot;&gt;Email&lt;/label&gt;
                        &lt;input type=&quot;email&quot; @@bind=&quot;email&quot;
                            class=&quot;w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500&quot;
                            placeholder=&quot;you@example.com&quot; /&gt;
                    &lt;/div&gt;
                    &lt;div&gt;
                        &lt;label class=&quot;block text-sm font-medium text-gray-700 mb-2&quot;&gt;Password&lt;/label&gt;
                        &lt;input type=&quot;password&quot; @@bind=&quot;password&quot;
                            @@onkeydown=&quot;@@(e =&gt; { if (e.Key == &quot;Enter&quot;) HandleSignIn(); })&quot;
                            class=&quot;w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500&quot;
                            placeholder=&quot;Enter your password&quot; /&gt;
                    &lt;/div&gt;
                &lt;/div&gt;

                &lt;button @@onclick=&quot;HandleSignIn&quot;
                    class=&quot;w-full mt-6 bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 rounded-lg transition-colors&quot;&gt;
                    Sign In
                &lt;/button&gt;

                &lt;div class=&quot;relative my-6&quot;&gt;
                    &lt;div class=&quot;absolute inset-0 flex items-center&quot;&gt;
                        &lt;div class=&quot;w-full border-t border-gray-300&quot;&gt;&lt;/div&gt;
                    &lt;/div&gt;
                    &lt;div class=&quot;relative flex justify-center text-sm&quot;&gt;
                        &lt;span class=&quot;px-2 bg-white text-gray-500&quot;&gt;Or continue with&lt;/span&gt;
                    &lt;/div&gt;
                &lt;/div&gt;

                &lt;button @@onclick=&quot;HandlePasskeySignIn&quot; disabled=&quot;@@isPasskeyAuthenticating&quot;
                    class=&quot;w-full flex items-center justify-center gap-2 px-4 py-3 border-2 border-gray-200 rounded-lg hover:border-blue-400 hover:shadow-lg transition-all duration-200 text-gray-700 font-medium disabled:opacity-50 disabled:cursor-not-allowed&quot;&gt;
                    @@if (isPasskeyAuthenticating)
                    {
                        &lt;svg class=&quot;animate-spin h-5 w-5 text-blue-600&quot; xmlns=&quot;http://www.w3.org/2000/svg&quot; fill=&quot;none&quot;
                            viewBox=&quot;0 0 24 24&quot;&gt;
                            &lt;circle class=&quot;opacity-25&quot; cx=&quot;12&quot; cy=&quot;12&quot; r=&quot;10&quot; stroke=&quot;currentColor&quot; stroke-width=&quot;4&quot;&gt;
                            &lt;/circle&gt;
                            &lt;path class=&quot;opacity-75&quot; fill=&quot;currentColor&quot;
                                d=&quot;M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z&quot;&gt;
                            &lt;/path&gt;
                        &lt;/svg&gt;
                        &lt;span&gt;Authenticating...&lt;/span&gt;
                    }
                    else
                    {
                        &lt;svg class=&quot;w-5 h-5&quot; fill=&quot;none&quot; stroke=&quot;currentColor&quot; viewBox=&quot;0 0 24 24&quot;&gt;
                            &lt;path stroke-linecap=&quot;round&quot; stroke-linejoin=&quot;round&quot; stroke-width=&quot;2&quot;
                                d=&quot;M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z&quot; /&gt;
                        &lt;/svg&gt;
                        &lt;span&gt;Sign in with Passkey&lt;/span&gt;
                    }
                &lt;/button&gt;

                &lt;div class=&quot;mt-6 text-center&quot;&gt;
                    &lt;p class=&quot;text-sm text-gray-600&quot;&gt;
                        Don't have an account?
                        &lt;a href=&quot;/signup&quot; class=&quot;text-blue-600 hover:text-blue-700 font-medium&quot;&gt;Sign Up&lt;/a&gt;
                    &lt;/p&gt;
                &lt;/div&gt;
            &lt;/div&gt;

            &lt;!-- Right: Demo Accounts --&gt;
            &lt;div
                class=&quot;bg-white rounded-2xl border-2 border-gray-200 p-6 sm:p-8 flex-1 h-full flex items-center flex-col justify-center&quot;&gt;
                &lt;h3 class=&quot;text-xl font-bold text-gray-900 mb-2&quot;&gt;Demo Accounts&lt;/h3&gt;
                &lt;p class=&quot;text-sm text-gray-600 mb-6&quot;&gt;Click any card to auto-fill credentials&lt;/p&gt;

                &lt;div class=&quot;space-y-3&quot;&gt;
                    &lt;!-- Admin Account --&gt;
                    &lt;button @@onclick=&quot;@@(() =&gt; SetDemoUser(&quot;admin@demo.com&quot;, &quot;admin123&quot;))&quot;
                        class=&quot;w-full text-left p-4 bg-white border-2 border-gray-200 rounded-xl hover:border-red-400 hover:shadow-lg transition-all duration-200&quot;&gt;
                        &lt;div class=&quot;flex items-center justify-center gap-3&quot;&gt;
                            &lt;div
                                class=&quot;h-10 w-10 rounded-full bg-red-600 flex items-center justify-center text-white font-semibold flex-shrink-0&quot;&gt;
                                AD
                            &lt;/div&gt;
                            &lt;div class=&quot;flex-1 min-w-0&quot;&gt;
                                &lt;div class=&quot;flex items-center gap-2 mb-1&quot;&gt;
                                    &lt;p class=&quot;text-sm font-semibold text-gray-900&quot;&gt;Admin User&lt;/p&gt;
                                    &lt;span
                                        class=&quot;px-2 py-0.5 text-xs font-medium bg-red-100 text-red-800 rounded&quot;&gt;Admin&lt;/span&gt;
                                &lt;/div&gt;
                                &lt;p class=&quot;text-xs text-gray-600 mb-1&quot;&gt;admin@demo.com&lt;/p&gt;
                                &lt;p class=&quot;text-xs text-gray-500&quot;&gt;Full system access • User management&lt;/p&gt;
                            &lt;/div&gt;
                        &lt;/div&gt;
                    &lt;/button&gt;

                    &lt;!-- Paid Account --&gt;
                    &lt;button @@onclick=&quot;@@(() =&gt; SetDemoUser(&quot;paid@demo.com&quot;, &quot;paid123&quot;))&quot;
                        class=&quot;w-full text-left p-4 bg-white border-2 border-gray-200 rounded-xl hover:border-purple-400 hover:shadow-lg transition-all duration-200&quot;&gt;
                        &lt;div class=&quot;flex items-center justify-center gap-3&quot;&gt;
                            &lt;div
                                class=&quot;h-10 w-10 rounded-full bg-purple-600 flex items-center justify-center text-white font-semibold flex-shrink-0&quot;&gt;
                                PU
                            &lt;/div&gt;
                            &lt;div class=&quot;flex-1 min-w-0&quot;&gt;
                                &lt;div class=&quot;flex items-center gap-2 mb-1&quot;&gt;
                                    &lt;p class=&quot;text-sm font-semibold text-gray-900&quot;&gt;Paid User&lt;/p&gt;
                                    &lt;span
                                        class=&quot;px-2 py-0.5 text-xs font-medium bg-purple-100 text-purple-800 rounded&quot;&gt;Paid&lt;/span&gt;
                                &lt;/div&gt;
                                &lt;p class=&quot;text-xs text-gray-600 mb-1&quot;&gt;paid@demo.com&lt;/p&gt;
                                &lt;p class=&quot;text-xs text-gray-500&quot;&gt;Advanced analytics • Premium features&lt;/p&gt;
                            &lt;/div&gt;
                        &lt;/div&gt;
                    &lt;/button&gt;

                    &lt;!-- Free Account --&gt;
                    &lt;button @@onclick=&quot;@@(() =&gt; SetDemoUser(&quot;user@demo.com&quot;, &quot;user123&quot;))&quot;
                        class=&quot;w-full text-left p-4 bg-white border-2 border-gray-200 rounded-xl hover:border-blue-400 hover:shadow-lg transition-all duration-200&quot;&gt;
                        &lt;div class=&quot;flex items-center justify-center gap-3&quot;&gt;
                            &lt;div
                                class=&quot;h-10 w-10 rounded-full bg-gray-600 flex items-center justify-center text-white font-semibold flex-shrink-0&quot;&gt;
                                FU
                            &lt;/div&gt;
                            &lt;div class=&quot;flex-1 min-w-0&quot;&gt;
                                &lt;div class=&quot;flex items-center gap-2 mb-1&quot;&gt;
                                    &lt;p class=&quot;text-sm font-semibold text-gray-900&quot;&gt;Free User&lt;/p&gt;
                                    &lt;span
                                        class=&quot;px-2 py-0.5 text-xs font-medium bg-gray-100 text-gray-800 rounded&quot;&gt;Free&lt;/span&gt;
                                &lt;/div&gt;
                                &lt;p class=&quot;text-xs text-gray-600 mb-1&quot;&gt;user@demo.com&lt;/p&gt;
                                &lt;p class=&quot;text-xs text-gray-500&quot;&gt;Basic metrics • Limited access&lt;/p&gt;
                            &lt;/div&gt;
                        &lt;/div&gt;
                    &lt;/button&gt;
                &lt;/div&gt;
            &lt;/div&gt;
        &lt;/div&gt;
    &lt;/div&gt;
&lt;/div&gt;</pre>
            </div>
            <p class="text-xs text-gray-600">
                This is still a demo-only auth flow. A production build should hash passwords, use ASP.NET Core Identity, and authenticate against a persistent data store.
            </p>

            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mt-6">
                <div class="text-xs text-gray-500 mb-1">Components/Pages/Demo/DashboardDemo/Auth/SignIn.razor.cs</div>
                <p class="text-xs text-gray-600 mb-2">Shared code-behind that injects <code>IUserAuthService</code> and handles all sign-in logic, including the passkey simulator.</p>
                <pre data-code-title="Components/Pages/Demo/DashboardDemo/Auth/SignIn.razor.cs"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">using Microsoft.AspNetCore.Components;
using AdminDashboard.Services;

namespace AdminDashboard.Components.Pages.Auth;

public partial class SignInBase : ComponentBase
{
    [Inject] protected IUserAuthService Auth { get; set; } = default!;
    [Inject] protected NavigationManager Nav { get; set; } = default!;

    protected string email = string.Empty;
    protected string password = string.Empty;
    protected string errorMessage = string.Empty;
    protected bool isPasskeyAuthenticating = false;
    protected string passkeyEmail = "admin@demo.com";
    protected string passkeyPassword = "admin123";

    protected void HandleSignIn()
    {
        errorMessage = string.Empty;

        if (string.IsNullOrWhiteSpace(email) || string.IsNullOrWhiteSpace(password))
        {
            errorMessage = "Please enter both email and password.";
            return;
        }

        if (Auth.SignIn(email, password))
        {
            Nav.NavigateTo("/profile");
        }
        else
        {
            errorMessage = "Invalid email or password.";
        }
    }

    protected async Task HandlePasskeySignIn()
    {
        errorMessage = string.Empty;
        isPasskeyAuthenticating = true;
        StateHasChanged();

        // Simulate passkey authentication delay
        await Task.Delay(1500);

        // Use the selected passkey user
        if (Auth.SignIn(passkeyEmail, passkeyPassword))
        {
            Nav.NavigateTo("/profile");
        }
        else
        {
            errorMessage = "Passkey authentication failed.";
            isPasskeyAuthenticating = false;
            StateHasChanged();
        }
    }

    protected void SetDemoUser(string demoEmail, string demoPassword)
    {
        email = demoEmail;
        password = demoPassword;
        passkeyEmail = demoEmail;
        passkeyPassword = demoPassword;
        errorMessage = string.Empty;
        StateHasChanged();
    }
}
</pre>
            </div>
        </div>

        <!-- How to do it (Gradient Banner) -->
        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">🛠️ How to do it</h2>
            <ol class="list-decimal ml-5 space-y-2 text-gray-700 text-sm sm:text-base">
                <li><strong>Create the file:</strong> Add <code>Components/Pages/Demo/DashboardDemo/Auth/SignIn.razor</code> (or the equivalent in your app) and paste in the markup above.</li>
                    <li><strong>Reference the shared base:</strong> Use <code>@@using AdminDashboard.Components.Pages.Auth</code> and <code>@@inherits SignInBase</code> so you get the shared auth helpers.</li>
                <li><strong>Wire two-way binding:</strong> Bind the email and password inputs to the backing fields declared in <code>SignInBase</code>.</li>
                <li><strong>Surface errors:</strong> Render the inline red alert whenever <code>errorMessage</code> has a value so learners see identical messaging.</li>
                <li><strong>Hook up buttons:</strong> Point the primary CTA at <code>HandleSignIn</code> and the secondary CTA at <code>HandlePasskeySignIn</code>.</li>
                <li><strong>Implement passkey UX:</strong> Keep the spinner/disabled state by toggling <code>isPasskeyAuthenticating</code> exactly like the demo.</li>
                <li><strong>Auto-fill demo users:</strong> Use <code>SetDemoUser</code> so each card updates the email/password fields and the passkey defaults.</li>
                <li><strong>Test loop:</strong> Sign up in Step 3, then sign in here and confirm you're redirected to <code>/profile</code>.</li>
            </ol>
        </div>

        <!-- Live Demo -->
        <div class="bg-black/3 rounded-2xl p-6 mb-6">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">🎬 Live Demo</h2>
            <p class="text-sm text-gray-600 mb-4">
                Here's the actual <code>/signin</code> experience with passkey CTA and demo account shortcuts:
            </p>
            <div class="bg-white rounded-2xl border border-gray-200 overflow-hidden">
                <div class="max-w-md mx-auto p-8">
                    <h2 class="text-3xl font-bold text-gray-900 mb-2">Sign In</h2>
                    <p class="text-gray-600 mb-6">Welcome back! Use your real or demo credentials.</p>

                    <div class="space-y-4">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                            <input type="email" placeholder="you@example.com"
                                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
                        </div>

                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Password</label>
                            <input type="password" placeholder="••••••••"
                                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
                        </div>

                        <button
                            class="w-full py-3 bg-blue-600 text-white rounded-lg font-semibold hover:bg-blue-700 transition-colors">
                            Sign In
                        </button>
                    </div>

                    <div class="relative my-5">
                        <div class="absolute inset-0 flex items-center">
                            <div class="w-full border-t border-gray-200"></div>
                        </div>
                        <div class="relative flex justify-center text-xs uppercase tracking-wide text-gray-500">
                            <span class="px-3 bg-white">Or continue with</span>
                        </div>
                    </div>

                    <button
                        class="w-full flex items-center justify-center gap-2 px-4 py-3 border-2 border-gray-200 rounded-lg hover:border-blue-400 transition">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
                        </svg>
                        Sign in with Passkey
                    </button>
                </div>

                <div class="bg-gray-50 border-t border-gray-200 px-6 py-4">
                    <p class="text-xs text-gray-600">
                        💡 <strong>Try it live:</strong> <a href="/signin"
                            class="text-blue-600 hover:text-blue-700 font-medium">Open /signin →</a> to auto-fill demo accounts and verify redirect behavior.
                    </p>
                </div>
            </div>
        </div>

        <!-- Mark Complete Section -->
        <div class="bg-white rounded-xl p-6 border-2 border-green-200 mb-6">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div>
                    <h3 class="text-lg font-semibold text-gray-900 mb-1">Finished Step 4?</h3>
                    <p class="text-sm text-gray-600">Mark this step as complete to track your Admin Dashboard learning progress.</p>
                </div>
                @if (isComplete)
                {
                    <div class="flex items-center gap-3">
                        <span class="px-4 py-2 rounded-full bg-green-100 text-green-700 font-medium">✓ Completed</span>
                        <button @onclick="ResetStep"
                            class="px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors text-sm">Reset</button>
                    </div>
                }
                else
                {
                    <button @onclick="MarkComplete"
                        class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg font-medium transition-colors">
                        Mark as Complete
                    </button>
                }
            </div>
        </div>

        <!-- Navigation -->
        <div class="mt-8">
            <div class="bg-black/90 rounded-2xl p-6 sm:p-8 text-white text-center">
                <p class="text-lg sm:text-xl font-semibold mb-4">Continue Learning</p>
                <p class="text-sm text-gray-300 mb-6 max-w-2xl mx-auto">
                    With <code>/signin</code> mirrored, move on to Step 5 to rebuild the <code>/profile</code> page exactly like the demo.
                </p>
                <div class="flex flex-col sm:flex-row items-center justify-center gap-3">
                    <a href="/admin-dashboard-guide"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 rounded-xl transition-colors">
                        <span class="text-lg">←</span>
                        <span class="text-sm sm:text-base font-semibold">Back to Guide</span>
                    </a>
                    <a href="/admin-dashboard-examples/step3"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 rounded-xl transition-colors">
                        <span class="text-lg">←</span>
                        <span class="text-sm sm:text-base font-semibold">Previous: Step 3</span>
                    </a>
                    <a href="/admin-dashboard-examples/step5"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700 rounded-xl transition-colors">
                        <span class="text-sm sm:text-base font-semibold">Next: Step 5</span>
                        <span class="text-lg">→</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>