@page "/admin-examples/step3"
@rendermode InteractiveServer
@inherits ExampleBase

<PageTitle>Admin Step 3: Status Badges & Dashboard Shell - Example</PageTitle>

<div class="min-h-screen py-8 rounded-4xl bg-gray-50">
    <div class="w-full mx-auto px-4 max-w-5xl">
        <a href="/admin-guide"
           class="inline-flex items-center gap-2 text-sm sm:text-base text-blue-600 hover:text-blue-700 mb-4">
            <span>←</span> Back to Admin Guide
        </a>

        <h1 class="text-4xl font-bold text-gray-900 mb-4">
            🎯 Step 3: Status Badges & Dashboard Shell
        </h1>

        <p class="text-xl text-gray-600 mb-8">
            In this step you add simple status badges and a grid-based dashboard shell that will hold your subscription, billing, and profile panels on the final <code class="text-[11px]">/admin/dashboard</code> page.
        </p>

        <div class="bg-gradient-to-r from-blue-100 to-purple-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">What You'll Learn</h2>
            <p class="text-gray-700 mb-4 text-sm sm:text-base">
                You’ll create small, semantic status badges for subscription state and wire up a simple dashboard shell layout where your stat cards and future panels will live.
            </p>
            <ul class="list-disc ml-6 text-sm sm:text-base text-gray-700 space-y-2 mb-4">
                <li>Design status badges that clearly communicate Active, Trial, and Overdue states.</li>
                <li>Build a responsive grid shell that keeps cards and panels aligned.</li>
                <li>Combine the Step 1 layout and Step 2 stat cards into a real dashboard frame.</li>
                <li>Prepare dedicated slots for Subscription, Billing, and Profile panels used in later steps.</li>
            </ul>

            <div class="mt-2 pt-3 border-t border-blue-200">
                <p class="text-xs sm:text-sm text-gray-700 mb-2">
                    <strong>💡 Key Concepts:</strong> SaaS status badges, dashboard shells, card grids
                </p>
                <div class="flex flex-wrap items-center gap-2">
                    <span class="text-xs sm:text-sm text-gray-600">→ Related tips:</span>
                    <a href="/tips#saas-admin-dashboard-layout"
                       class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">SaaS Admin Dashboard Layout</a>
                    <a href="/tips#semantic-status-badges"
                       class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Semantic Status Badges</a>
                    <a href="/tips#reusable-stat-cards"
                       class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Reusable Stat Cards</a>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl font-bold text-gray-900 mb-4">📊 Sample Component: AdminDashboardShellExample</h2>
            <p class="text-sm text-gray-600 mb-4">
                <code class="text-[11px]">AdminDashboardShellExample</code> shows a simple dashboard frame with a title row, a row of stat cards, and reserved slots for subscription, billing, and profile panels, plus a set of status badges for the current subscription.
            </p>

            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mb-4">
                <div class="text-xs text-gray-500 mb-1">Components/Pages/Examples/Admin/Step3/AdminDashboardShellExample.razor</div>
                <p class="text-xs text-gray-600 mb-2">Razor markup that composes stat cards and status badges into a dashboard grid shell.</p>
                <pre data-code-title="Razor (AdminDashboardShellExample.razor)"
                     class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">@@page &quot;/admin-examples/demo-dashboard-shell&quot;
@@rendermode InteractiveServer
@@inherits AdminDashboardShellExampleBase

&lt;h3 class=&quot;text-xl font-semibold mb-3&quot;&gt;Admin Dashboard Shell&lt;/h3&gt;

&lt;div class=&quot;space-y-4&quot;&gt;
    &lt;div class=&quot;flex flex-wrap items-center justify-between gap-3&quot;&gt;
        &lt;div&gt;
            &lt;p class=&quot;text-xs text-gray-500&quot;&gt;Project&lt;/p&gt;
            &lt;p class=&quot;text-sm font-semibold text-gray-900&quot;&gt;SaaS Billing Admin&lt;/p&gt;
        &lt;/div&gt;
        &lt;div class=&quot;flex items-center gap-2&quot;&gt;
            &lt;AdminStatusBadge Status=&quot;active&quot; /&gt;
            &lt;AdminStatusBadge Status=&quot;trial&quot; /&gt;
            &lt;AdminStatusBadge Status=&quot;overdue&quot; /&gt;
        &lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;grid gap-4 sm:grid-cols-3&quot;&gt;
        &lt;AdminStatCard
            Title=&quot;Monthly Recurring Revenue&quot;
            Value=&quot;$24,800&quot;
            HelperText=&quot;+8.2% vs last month&quot;
            Status=&quot;good&quot; /&gt;

        &lt;AdminStatCard
            Title=&quot;Active Subscriptions&quot;
            Value=&quot;1,204&quot;
            HelperText=&quot;Starter, Growth, and Scale plans&quot;
            Status=&quot;neutral&quot; /&gt;

        &lt;AdminStatCard
            Title=&quot;Churn This Month&quot;
            Value=&quot;2.1%&quot;
            HelperText=&quot;Watching accounts at risk closely&quot;
            Status=&quot;warning&quot; /&gt;
    &lt;/div&gt;

    &lt;div class=&quot;grid gap-4 md:grid-cols-[minmax(0,2fr),minmax(0,1.4fr)]&quot;&gt;
        &lt;div class=&quot;bg-white rounded-xl p-4 border border-gray-200 h-40 flex items-center justify-center text-xs text-gray-500&quot;&gt;
            Subscription panel slot (Step 8)
        &lt;/div&gt;
        &lt;div class=&quot;bg-white rounded-xl p-4 border border-gray-200 h-40 flex items-center justify-center text-xs text-gray-500&quot;&gt;
            Billing summary panel slot (Step 9)
        &lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;bg-white rounded-xl p-4 border border-gray-200 h-40 flex items-center justify-center text-xs text-gray-500&quot;&gt;
        Profile settings panel slot (Step 10)
    &lt;/div&gt;
&lt;/div&gt;</pre>
            </div>

            <h3 class="text-sm font-semibold text-gray-800 mb-2">Step-by-step breakdown</h3>
            <div class="grid gap-4">
                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4">
                    <div class="text-xs text-gray-500 mb-1">1. Add simple status badges</div>
                    <p class="text-xs text-gray-600 mb-2">Status badges make it easy to scan whether a subscription is active, in trial, or overdue.</p>
                    <ul class="list-disc ml-4 mb-2 text-[11px] text-gray-600 space-y-1">
                        <li>Use short labels like &quot;Active&quot;, &quot;Trial&quot;, and &quot;Overdue&quot;.</li>
                        <li>Pair each label with a distinct color so students can recognize states quickly.</li>
                        <li>Keep the badge component tiny so it can appear in multiple places.</li>
                    </ul>
                    <pre class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">&lt;div class=&quot;inline-flex items-center gap-1 px-2 py-1 rounded-full text-[11px] font-medium&quot;&gt;
    &lt;span class=&quot;h-1.5 w-1.5 rounded-full bg-emerald-500&quot;&gt;&lt;/span&gt;
    &lt;span&gt;Active&lt;/span&gt;
&lt;/div&gt;</pre>
                </div>

                <div class="bg-gray-100 border border-gray-200 rounded-lg p-4">
                    <div class="text-xs text-gray-500 mb-1">2. Define a dashboard grid shell</div>
                    <p class="text-xs text-gray-600 mb-2">The shell arranges tiles and panels in a predictable grid so future steps can plug in real content.</p>
                    <ul class="list-disc ml-4 mb-2 text-[11px] text-gray-600 space-y-1">
                        <li>Use a three-column row for stat cards to mirror many SaaS dashboards.</li>
                        <li>Reserve left and right columns for subscription and billing content.</li>
                        <li>Leave a full-width row for profile settings and other details.</li>
                    </ul>
                    <pre class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">&lt;div class=&quot;grid gap-4 md:grid-cols-[minmax(0,2fr),minmax(0,1.4fr)]&quot;&gt;
    &lt;div&gt;Subscription panel slot&lt;/div&gt;
    &lt;div&gt;Billing summary panel slot&lt;/div&gt;
&lt;/div&gt;</pre>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">📝 Important Notes &amp; Tips</h2>
            <div class="space-y-3">
                <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                    <h3 class="text-sm font-bold text-blue-900 mb-3">💡 Pro Tips</h3>
                    <div class="space-y-4">
                        <div>
                            <p class="text-sm font-semibold text-blue-900 mb-2">Keep tiles and panels predictable</p>
                            <p class="text-xs text-blue-800">Use a consistent grid so students always know where metrics, subscription details, and profile settings live. This mirrors real SaaS admin layouts.</p>
                        </div>
                        <div>
                            <p class="text-sm font-semibold text-blue-900 mb-2">Use badges instead of raw text</p>
                            <p class="text-xs text-blue-800">Short, color-coded badges are easier to scan than plain text labels like &quot;Status: Active&quot;. They also give you a single component to tweak later.</p>
                        </div>
                    </div>
                </div>

                <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
                    <h3 class="text-sm font-bold text-yellow-900 mb-2">🔧 Troubleshooting</h3>
                    <ul class="list-disc ml-6 text-sm text-yellow-900 space-y-1">
                        <li>If the grid feels cramped, double-check your padding and <code class="text-[11px]">gap-4</code> spacing on the outer containers.</li>
                        <li>If badges don’t line up, ensure they’re wrapped in a flex container with small gaps instead of being spaced manually.</li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="bg-gradient-to-r from-blue-100 to-purple-100 rounded-xl p-6 mb-6">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">🛠️ How to do it</h2>
            <ol class="list-decimal ml-5 space-y-2 text-gray-700">
                <li>
                    <strong>Create the dashboard shell component:</strong>
                    Add <code>AdminDashboardShellExample.razor</code> and <code>AdminDashboardShellExample.razor.cs</code> under
                    <code>Components/Pages/Examples/Admin/Step3</code>, following the sample above.
                </li>
                <li>
                    <strong>Add status badges:</strong>
                    Implement a tiny <code>AdminStatusBadge</code> component that takes a status string and renders a matching label + color.
                </li>
                <li>
                    <strong>Reserve panel slots:</strong>
                    Create placeholder boxes for Subscription, Billing, and Profile panels so future steps can replace them with real components.
                </li>
            </ol>
        </div>

        <div class="bg-black/3 rounded-2xl p-6 mb-6">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">🎬 Live Demo</h2>
            <p class="text-sm text-gray-600 mb-4">Use the demo below to see how stat cards, badges, and panel slots come together into a single dashboard shell.</p>
            <div class="bg-white rounded-2xl border border-gray-200 p-6">
                <AdminDashboardShellExample />
            </div>
            <p class="text-xs text-gray-600 mt-4 text-center">💡 The production <code class="text-[11px]">/admin/dashboard</code> page will reuse this same shell and swap the placeholders for real panels.</p>
        </div>

        <div class="bg-white rounded-xl p-6 border-2 border-green-200 mb-6">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div>
                    <h3 class="text-lg font-semibold text-gray-900 mb-1">Finished Step 3?</h3>
                    <p class="text-sm text-gray-600">Mark this step complete once your dashboard shell shows stat cards, status badges, and placeholder slots for subscription, billing, and profile panels.</p>
                </div>
                @if (isComplete)
                {
                    <div class="flex items-center gap-3">
                        <span class="px-4 py-2 rounded-full bg-green-100 text-green-700 font-medium">✅ Completed</span>
                        <button @onclick="ResetStep"
                            class="px-4 py-2 border border-gray-300 rounded hover:bg-gray-50 transition-colors text-sm">Reset</button>
                    </div>
                }
                else
                {
                    <button @onclick="MarkComplete"
                        class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded font-medium transition-colors">Mark
                        as Complete</button>
                }
            </div>
        </div>

        <div class="mt-8">
            <div class="bg-black/90 rounded-2xl p-6 sm:p-8 text-white text-center">
                <p class="text-lg sm:text-xl font-semibold mb-4">You&apos;ve Finished the Admin Track 🎉</p>
                <p class="text-sm text-gray-300 mb-6 max-w-2xl mx-auto">
                    You now have a full, modern-feeling Admin experience: a layout shell, a simple auth service that
                    models passkeys, and a role-aware dashboard. From here you can plug in ASP.NET Core Identity or any
                    external IdP that issues the same kinds of claims.
                </p>
                <div class="flex flex-col sm:flex-row items-center justify-center gap-3">
                    <a href="/admin-guide"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 rounded-xl transition-colors">
                        <span class="text-lg">←</span>
                        <span class="text-sm sm:text-base font-semibold">Back to Guide</span>
                    </a>
                    <a href="/admin-examples/step2"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 rounded-xl transition-colors">
                        <span class="text-lg">←</span>
                        <span class="text-sm sm:text-base font-semibold">Previous: Step 2</span>
                    </a>
                    <a href="/"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700 rounded-xl transition-colors">
                        <span class="text-sm sm:text-base font-semibold">Back to Home</span>
                        <span class="text-lg">🏠</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>