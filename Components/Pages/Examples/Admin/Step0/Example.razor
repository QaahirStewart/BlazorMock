@page "/admin-examples/step0"
@rendermode InteractiveServer
@inherits ExampleBase

<PageTitle>Admin Step 0: Prerequisites & VS Code Setup - Example</PageTitle>

<div class="min-h-screen py-8 rounded-4xl bg-gray-50">
    <div class="w-full mx-auto px-4 max-w-5xl">
        <a href="/admin-guide"
            class="inline-flex items-center gap-2 text-sm sm:text-base text-blue-600 hover:text-blue-700 mb-4">
            <span>←</span> Back to SaaS Dashboard Guide
        </a>

        <h1 class="text-4xl font-bold text-gray-900 mb-4">
            🎯 Step 0: Prerequisites & VS Code Setup
        </h1>

        <p class="text-xl text-gray-600 mb-8">
            Get your machine ready with the core tools you'll use across all tracks: .NET, Node.js, npm, and VS Code
            with the right extensions.
        </p>

        <!-- What You'll Learn -->
        <div class="bg-gradient-to-r from-blue-100 to-purple-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">What You'll Learn</h2>

            <p class="text-gray-700 mb-4 text-sm sm:text-base">
                This step focuses on global prerequisites only. You\'ll install and verify .NET, Node.js, and npm, set
                up VS Code, and confirm that your tools are ready before you touch any specific project.
            </p>

            <ul class="list-disc ml-6 text-sm sm:text-base text-gray-700 space-y-2 mb-4">
                <li>Install the .NET SDK (10.0+), Node.js LTS, and npm.</li>
                <li>Install VS Code plus C#, Razor, and Tailwind CSS IntelliSense extensions.</li>
                <li>Verify your tools from the terminal with version commands.</li>
                <li>Confirm you can create and build .NET projects from the command line.</li>
            </ul>

            <div class="mt-2 pt-3 border-t border-blue-200">
                <p class="text-xs sm:text-sm text-gray-700 mb-2">
                    <strong>💡 Key Concepts:</strong> Environment setup, VS Code tooling, terminal basics
                </p>
                <div class="flex flex-wrap items-center gap-2">
                    <span class="text-xs sm:text-sm text-gray-600">→ Related tips:</span>
                    <a href="/tips#dotnet-cli"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">dotnet
                        CLI</a>
                    <a href="/tips#vs-code-extensions"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">VS
                        Code Extensions</a>
                    <a href="/tips#terminal-basics"
                        class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[11px] sm:text-xs font-medium hover:bg-blue-200 transition-colors">Terminal
                        Basics</a>
                </div>
            </div>
        </div>

        <!-- Sample Section: Environment Checks & Tooling -->
        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl font-bold text-gray-900 mb-4">🧩 Environment Checks & Tooling</h2>

            <p class="text-sm text-gray-600 mb-4">
                Use these commands to verify your environment and run the SaaS Dashboard project locally. You only need
                to do this once per machine before you start working through the rest of the steps.
            </p>

            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mb-4">
                <div class="text-xs text-gray-500 mb-1">Environment checks (PowerShell)</div>
                <p class="text-xs text-gray-600 mb-2">Confirm that .NET, Node.js, and npm are installed and available on
                    your PATH.</p>
                <ul class="list-disc ml-4 mb-3 text-[11px] text-gray-600 space-y-1">
                    <li>Run each command in a new terminal window.</li>
                    <li>Make sure the versions are at or above what the course requires.</li>
                    <li>If any command fails, install or update that tool before moving on.</li>
                </ul>
                <pre data-code-title="PowerShell (environment checks)"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">dotnet --info
node --version
npm --version</pre>
            </div>

            <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 overflow-x-auto mb-4">
                <div class="text-xs text-gray-500 mb-1">Install VS Code extensions</div>
                <p class="text-xs text-gray-600 mb-2">Make sure VS Code is ready to understand C#, Razor, and Tailwind
                    so all projects feel the same across tracks.</p>
                <ul class="list-disc ml-4 mb-3 text-[11px] text-gray-600 space-y-1">
                    <li>Install the official C# (Dev Kit) extension for .NET.</li>
                    <li>Install the Razor language tools if prompted.</li>
                    <li>Install a Tailwind CSS IntelliSense extension for class name help.</li>
                </ul>
                <pre data-code-title="VS Code Marketplace (extensions)"
                    class="code-block text-xs sm:text-sm font-mono text-gray-800 w-full max-w-full overflow-x-auto">C# Dev Kit
Razor Language Support
Tailwind CSS IntelliSense</pre>
            </div>
        </div>

        <!-- Important Notes & Tips -->
        <div class="bg-white rounded-xl p-6 border border-gray-200 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">📝 Important Notes & Tips</h2>

            <div class="space-y-3">
                <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                    <h3 class="text-sm font-bold text-blue-900 mb-3">💡 Pro Tips</h3>
                    <ul class="list-disc ml-6 text-sm text-blue-900 space-y-1">
                        <li>Keep one terminal tab for running <code>dotnet run</code> and another for issuing commands.
                        </li>
                        <li>Install recommended VS Code extensions when prompted so Razor, C#, and Tailwind all light up
                            correctly.</li>
                        <li>If you\'re on Windows, make sure you\'re using a PowerShell terminal like the examples.</li>
                    </ul>
                </div>

                <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
                    <h3 class="text-sm font-bold text-yellow-900 mb-2">⚠️ Troubleshooting</h3>
                    <ul class="list-disc ml-6 text-sm text-yellow-900 space-y-1">
                        <li>If <code>dotnet --version</code> fails, install the latest .NET SDK from the official
                            download page.</li>
                        <li>If <code>dotnet run</code> fails, run <code>dotnet restore</code> once, then try again.</li>
                        <li>If VS Code doesn\'t detect .NET, fully close VS Code and reopen it from a terminal using
                            <code>code .</code>.
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- How to do it -->
        <div class="bg-gradient-to-r from-blue-100 to-purple-100 rounded-xl p-6 mb-6">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">🛠️ How to do it</h2>
            <ol class="list-decimal ml-5 space-y-2 text-gray-700">
                <li>Open a PowerShell terminal and run the environment check commands to confirm .NET, Node.js, and npm
                    are installed.</li>
                <li>Install VS Code and the recommended extensions for C#, Razor, and Tailwind CSS.</li>
                <li>Use <code>dotnet new blazor</code> in a throwaway folder if you want to double-check that new
                    projects build and run.</li>
                <li>Once everything is installed and the sample project runs, mark this step complete.</li>
                <li>Next, go to Step 1 to create a new clean SaaS Dashboard project from scratch.</li>
            </ol>
        </div>

        <!-- Mark Complete -->
        <div class="bg-white rounded-xl p-6 border-2 border-green-200 mb-6">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div>
                    <h3 class="text-lg font-semibold text-gray-900 mb-1">Ready to build the dashboard?</h3>
                    <p class="text-sm text-gray-600">Once your environment is ready and you can create test projects,
                        mark this
                        step complete.</p>
                </div>
                @if (isComplete)
                {
                    <div class="flex items-center gap-3">
                        <span class="px-4 py-2 rounded-full bg-green-100 text-green-700 font-medium">✓ Completed</span>
                        <button @onclick="ResetStep"
                            class="px-4 py-2 border border-gray-300 rounded hover:bg-gray-50 transition-colors text-sm">Reset</button>
                    </div>
                }
                else
                {
                    <button @onclick="MarkComplete"
                        class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded font-medium transition-colors">
                        Mark as Complete
                    </button>
                }
            </div>
        </div>

        <!-- Navigation -->
        <div class="mt-8">
            <div class="bg-black/90 rounded-2xl p-6 sm:p-8 text-white text-center">
                <p class="text-lg sm:text-xl font-semibold mb-4">Continue Learning</p>
                <p class="text-sm text-gray-300 mb-6 max-w-2xl mx-auto">
                    Your environment is ready. Next, you'll create a new clean SaaS Dashboard project using the .NET CLI
                    and set up Tailwind CSS for modern styling.
                </p>
                <div class="flex flex-col sm:flex-row items-center justify-center gap-3">
                    <a href="/admin-guide"
                        class="inline-flex items-center gap-2 px-6 py-3 border-2 border-gray-200 rounded text-sm sm:text-base hover:bg-white/10 transition-colors">
                        <span class="text-lg">←</span>
                        <span>Back to guide</span>
                    </a>
                    <a href="/admin-examples/step1"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700 rounded text-sm sm:text-base font-semibold transition-colors">
                        <span>Next: New Clean Project</span>
                        <span class="text-lg">→</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>