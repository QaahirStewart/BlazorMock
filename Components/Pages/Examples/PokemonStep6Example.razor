@page "/pokemon-examples/step6"
@inject ILearningProgressService ProgressService
@inject NavigationManager Navigation
@inject IJSRuntime JS
@implements IDisposable
@rendermode InteractiveServer

<PageTitle>Pokemon Step 6: Search, Filter & Details</PageTitle>

<div class="min-h-screen py-8">
    <div class="w-full mx-auto px-4 max-w-5xl">
        <a href="/pokemon-guide" class="inline-flex items-center gap-2 text-blue-600 hover:text-blue-700 mb-4">
            <span>←</span> Back to Pokemon Guide
        </a>

        <div class="rounded-2xl overflow-hidden border-2 border-gray-200 bg-white mb-6">
            <div class="h-2 w-full bg-gradient-to-r from-blue-200 via-blue-300 to-emerald-200"></div>
            <div class="p-5 sm:p-6">
                <h1 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-2">
                    🔍 Step 6: Search, Filter & Details
                </h1>
                <p class="text-gray-600">
                    Add search functionality, type filtering, and a detailed Pokemon modal view.
                </p>
            </div>
        </div>

        <div class="bg-gradient-to-r from-gray-100 to-blue-100 rounded-2xl p-5 sm:p-6 mb-6">
            <h2 class="text-xl sm:text-2xl font-semibold text-gray-800 mb-4">What You'll Learn</h2>
            <ul class="list-disc ml-6 text-sm sm:text-base text-gray-700 space-y-2">
                <li>Implement real-time search filtering</li>
                <li>Fetch individual Pokemon details from PokeAPI</li>
                <li>Display Pokemon stats, types, and abilities</li>
                <li>Create a modal detail view</li>
                <li>Reset to page 1 when filtering</li>
            </ul>
        </div>

        <div class="bg-white rounded-2xl p-5 sm:p-6 mb-6 border-2 border-gray-200">
            <h2 class="text-xl font-bold text-gray-900 mb-4">📝 Code Example</h2>

            <div class="mb-4">
                <h3 class="text-sm font-semibold text-gray-700 mb-2">1. Add search state</h3>
                <pre class="bg-gray-100 p-4 rounded-lg text-xs sm:text-sm font-mono overflow-x-auto">@@code {
    private string searchQuery = "";
    private PokemonDetail? selectedPokemon;
    
    private List&lt;PokemonItem&gt; GetFilteredPokemon()
    {
        if (string.IsNullOrWhiteSpace(searchQuery))
            return allPokemon;
            
        return allPokemon
            .Where(p => p.Name.Contains(searchQuery, 
                   StringComparison.OrdinalIgnoreCase))
            .ToList();
    }
    
    private List&lt;PokemonItem&gt; GetPagedPokemon()
    {
        var filtered = GetFilteredPokemon();
        return filtered
            .Skip((currentPage - 1) * pageSize)
            .Take(pageSize)
            .ToList();
    }
}</pre>
            </div>

            <div class="mb-4">
                <h3 class="text-sm font-semibold text-gray-700 mb-2">2. Search input UI</h3>
                <pre class="bg-gray-100 p-4 rounded-lg text-xs sm:text-sm font-mono overflow-x-auto">Create search input with:
- @@bind="searchQuery"
- @@bind:event="oninput" (update as user types)
- @@bind:after="OnSearchChanged" (reset pagination)</pre>
            </div>

            <div class="mb-4">
                <h3 class="text-sm font-semibold text-gray-700 mb-2">3. Reset to page 1 on search</h3>
                <pre class="bg-gray-100 p-4 rounded-lg text-xs sm:text-sm font-mono overflow-x-auto">@@code {
    private void OnSearchChanged()
    {
        currentPage = 1; // Reset to first page
        totalPages = (int)Math.Ceiling(
            GetFilteredPokemon().Count / (double)pageSize
        );
    }
}</pre>
            </div>

            <div class="mb-4">
                <h3 class="text-sm font-semibold text-gray-700 mb-2">4. Fetch Pokemon details</h3>
                <pre class="bg-gray-100 p-4 rounded-lg text-xs sm:text-sm font-mono overflow-x-auto">private async Task ShowDetails(int id)
{
    try
    {
        selectedPokemon = await Http.GetFromJsonAsync&lt;PokemonDetail&gt;(
            $"https://pokeapi.co/api/v2/pokemon/{id}"
        );
    }
    catch (Exception ex)
    {
        Console.WriteLine($"Error loading Pokemon: {ex.Message}");
    }
}

public class PokemonDetail
{
    public int Id { get; set; }
    public string Name { get; set; } = "";
    public int Height { get; set; }
    public int Weight { get; set; }
    public int Base_Experience { get; set; }
    public List&lt;PokemonType&gt; Types { get; set; } = new();
    public List&lt;PokemonAbility&gt; Abilities { get; set; } = new();
    public PokemonStats Stats { get; set; } = new();
}

public class PokemonType
{
    public TypeInfo Type { get; set; } = new();
}

public class TypeInfo
{
    public string Name { get; set; } = "";
}

public class PokemonAbility
{
    public AbilityInfo Ability { get; set; } = new();
}

public class AbilityInfo
{
    public string Name { get; set; } = "";
}</pre>
            </div>

            <div class="mb-4">
                <h3 class="text-sm font-semibold text-gray-700 mb-2">5. Modal detail view</h3>
                <pre class="bg-gray-100 p-4 rounded-lg text-xs sm:text-sm font-mono overflow-x-auto">Modal structure:
1. Fixed overlay with @@onclick="CloseModal"
2. White content card with @@onclick:stopPropagation
3. Display Pokemon sprite, name, types, abilities
4. Show height/weight (divide API values by 10)
5. Loop through types and abilities to display</pre>
            </div>
        </div>

        <div class="bg-yellow-50 border-2 border-yellow-200 rounded-2xl p-5 sm:p-6 mb-6">
            <h3 class="text-lg font-bold text-yellow-900 mb-2">💡 Pro Tips</h3>
            <ul class="list-disc ml-6 text-sm text-yellow-900 space-y-1">
                <li>Use <code class="bg-white px-1 rounded">@@bind:after</code> to reset pagination when search changes
                </li>
                <li>Click outside modal to close: <code class="bg-white px-1 rounded">@@onclick</code> on overlay</li>
                <li>Prevent modal close on content click: <code
                        class="bg-white px-1 rounded">@@onclick:stopPropagation</code></li>
                <li>Height and weight in PokeAPI are in decimeters/hectograms - divide by 10 for display</li>
            </ul>
        </div>

        <div class="bg-white rounded-xl p-6 border-2 border-green-200 mb-6">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div>
                    <h3 class="text-lg font-semibold text-gray-900 mb-1">Finished Step 6?</h3>
                    <p class="text-sm text-gray-600">Congratulations! Your Pokemon collector is complete! 🎉</p>
                </div>
                @if (isComplete)
                {
                    <div class="flex items-center gap-3">
                        <span class="px-4 py-2 rounded-full bg-green-100 text-green-700 font-medium">✓ Completed</span>
                        <button @onclick="ResetStep"
                            class="px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors text-sm">Reset</button>
                    </div>
                }
                else
                {
                    <button @onclick="MarkComplete"
                        class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg font-medium transition-colors">Mark
                        as Complete</button>
                }
            </div>
        </div>

        <div class="bg-gradient-to-r from-yellow-200 to-emerald-200 rounded-2xl p-6 mb-6">
            <h3 class="text-xl font-bold text-gray-900 mb-2">🎊 Congratulations!</h3>
            <p class="text-gray-700 mb-3">
                You've built a complete Pokemon collector app with search, filtering, pagination, and details!
            </p>
            <div class="flex gap-3">
                <a href="/pokemon-demo"
                    class="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition-colors">
                    View Demo
                </a>
                <a href="/pokemon-guide"
                    class="px-4 py-2 bg-white hover:bg-gray-50 rounded-lg border-2 border-gray-200 transition-colors">
                    Back to Guide
                </a>
            </div>
        </div>

        <div class="flex items-center justify-between">
            <a href="/pokemon-examples/step5"
                class="inline-flex items-center gap-2 px-4 py-2 border-2 border-gray-200 rounded-lg hover:bg-gray-50 transition-colors">
                <span>←</span>
                Previous: Step 5
            </a>
            <a href="/pokemon-guide"
                class="inline-flex items-center gap-2 px-4 py-2 bg-emerald-600 hover:bg-emerald-700 text-white rounded-lg transition-colors">
                Complete Guide
                <span>✓</span>
            </a>
        </div>
    </div>
</div>

@code {
    private bool isComplete;
    private IJSObjectReference? _copyModule;

    protected override async Task OnInitializedAsync()
    {
        var step = await ProgressService.GetStepAsync("pokemon", 6);
        isComplete = step?.IsComplete ?? false;
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            try
            {
                _copyModule = await JS.InvokeAsync<IJSObjectReference>("import", "./js/codeblocks.js");
                await _copyModule.InvokeVoidAsync("enhancePreBlocks");
            }
            catch (Exception ex)
            {
                Console.WriteLine($"Failed to load code block enhancer: {ex.Message}");
            }
        }
    }

    private async Task MarkComplete()
    {
        await ProgressService.MarkStepCompleteAsync("pokemon", 6);
        isComplete = true;
    }

    private async Task ResetStep()
    {
        await ProgressService.MarkStepIncompleteAsync("pokemon", 6);
        isComplete = false;
    }

    public void Dispose()
    {
        _copyModule?.DisposeAsync();
    }
}