<header class="py-6">
    <div class="max-w-6xl mx-auto px-4">
        <nav class="bg-black/85 text-white rounded-2xl px-4 md:py-6 py-4 shadow-md">
            <div class="flex items-center justify-between">
                <!-- Brand -->
                <a href="/"
                    class="flex items-center gap-2 md:flex-initial flex-1 md:justify-start justify-center md:order-1 order-2">
                    <span class="text-sm sm:text-base font-semibold tracking-wide">AdminDashboard</span>
                </a>

                <!-- Hamburger button (mobile only) -->
                <button type="button" @onclick="ToggleMobileMenu"
                    class="md:hidden inline-flex items-center justify-center h-10 w-10 rounded-lg hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-white/30 order-1 transition-transform duration-200 @(isMobileMenuOpen ? "rotate-90" : string.Empty)"
                    aria-label="Toggle navigation" aria-expanded="@isMobileMenuOpen">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                        class="h-6 w-6 transition-all duration-300">
                        @if (isMobileMenuOpen)
                        {
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M6 18L18 6M6 6l12 12" />
                        }
                        else
                        {
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M4 6h16M4 12h16M4 18h16" />
                        }
                    </svg>
                </button>

                <!-- Desktop nav links -->
                <ul
                    class="hidden md:flex items-center justify-center gap-4 lg:gap-6 text-sm text-gray-200 order-2 absolute left-1/2 -translate-x-1/2">
                    <li>
                        <NavLink href="/" Match="NavLinkMatch.All" class="hover:text-white transition-colors"
                            ActiveClass="text-white font-semibold underline underline-offset-4">Overview</NavLink>
                    </li>
                    <li>
                        <NavLink href="/profile" class="hover:text-white transition-colors"
                            ActiveClass="text-white font-semibold underline underline-offset-4">Profile</NavLink>
                    </li>
                    <li>
                        <NavLink href="/analytics" class="hover:text-white transition-colors"
                            ActiveClass="text-white font-semibold underline underline-offset-4">Analytics</NavLink>
                    </li>
                    @if (Auth.IsAuthenticated && Auth.CurrentUser?.Role == "Admin")
                    {
                        <li>
                            <NavLink href="/admin/dashboard" class="hover:text-white transition-colors"
                                ActiveClass="text-white font-semibold underline underline-offset-4">Admin Dashboard
                            </NavLink>
                        </li>
                    }
                </ul>

                <!-- User Profile Icon / Sign In (desktop) -->
                <div class="hidden md:block order-3">
                    @if (Auth.IsAuthenticated && Auth.CurrentUser != null)
                    {
                        <div class="relative">
                            <button @onclick="ToggleProfileMenu"
                                class="flex items-center gap-2 px-3 py-2 rounded-lg hover:bg-white/10 transition-colors">
                                @if (!string.IsNullOrEmpty(Auth.CurrentUser.ProfilePictureUrl))
                                {
                                    <img src="@Auth.CurrentUser.ProfilePictureUrl" alt="@Auth.CurrentUser.FullName"
                                        class="h-8 w-8 rounded-full object-cover border-2 border-white/30" />
                                }
                                else
                                {
                                    <div
                                        class="h-8 w-8 rounded-full @GetUserAvatarColor() flex items-center justify-center text-white text-xs font-semibold border-2 border-white/30">
                                        @GetUserInitials()
                                    </div>
                                }
                                <span class="text-sm font-medium">@Auth.CurrentUser.FullName</span>
                                <svg class="w-4 h-4 transition-transform @(isProfileMenuOpen ? "rotate-180" : string.Empty)"
                                    fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M19 9l-7 7-7-7" />
                                </svg>
                            </button>

                            @if (isProfileMenuOpen)
                            {
                                <div
                                    class="absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-xl border border-gray-200 py-1 z-50">
                                    <a href="/profile" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">My
                                        Profile</a>
                                    <a href="/analytics"
                                        class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">Analytics</a>
                                    @if (Auth.CurrentUser.Role == "Admin")
                                    {
                                        <a href="/admin/dashboard"
                                            class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">Admin Dashboard</a>
                                    }
                                    <div class="border-t border-gray-200 my-1"></div>
                                    <button type="button" @onclick="HandleSignOut"
                                        class="block w-full text-left px-4 py-2 text-sm text-red-600 hover:bg-red-50">Sign
                                        Out</button>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <div class="flex items-center gap-2">
                            <a href="/signin" class="px-4 py-2 text-sm font-medium hover:bg-white/10 rounded-lg">Sign In</a>
                            <a href="/signup"
                                class="px-4 py-2 text-sm font-medium bg-white text-black rounded-lg hover:bg-gray-100">Sign
                                Up</a>
                        </div>
                    }
                </div>

                <!-- Spacer for mobile to keep logo centered -->
                <div class="md:hidden w-10 order-3"></div>
            </div>

            <!-- ============================================================ -->
            <!-- STEP 12: Mobile Menu Panel -->
            <!-- The collapsible drawer for phones goes right here, inside the <nav> tag -->
            <!-- It will slide down from under the header when someone taps the hamburger button -->
            <!-- ============================================================ -->
            <div
                class="md:hidden overflow-hidden transition-all duration-300 ease-in-out @(isMobileMenuOpen ? "max-h-[600px] opacity-100" : "max-h-0 opacity-0")">
                <div
                    class="mt-4 pt-4 border-t border-white/10 transform transition-transform duration-300 @(isMobileMenuOpen ? "translate-y-0" : "-translate-y-4")">
                    <nav class="flex flex-col gap-1">
                        <NavLink href="/" Match="NavLinkMatch.All" @onclick="CloseMobileMenu"
                            class="px-3 py-2 rounded-lg hover:bg-white/10 transition-colors"
                            ActiveClass="bg-white/10 font-semibold">
                            Overview
                        </NavLink>
                        <NavLink href="/profile" @onclick="CloseMobileMenu"
                            class="px-3 py-2 rounded-lg hover:bg-white/10 transition-colors"
                            ActiveClass="bg-white/10 font-semibold">
                            Profile
                        </NavLink>
                        <NavLink href="/analytics" @onclick="CloseMobileMenu"
                            class="px-3 py-2 rounded-lg hover:bg-white/10 transition-colors"
                            ActiveClass="bg-white/10 font-semibold">
                            Analytics
                        </NavLink>

                        @if (Auth.IsAuthenticated && Auth.CurrentUser != null)
                        {
                            @if (Auth.CurrentUser.Role == "Admin")
                            {
                                <NavLink href="/admin/dashboard" @onclick="CloseMobileMenu"
                                    class="px-3 py-2 rounded-lg hover:bg-white/10 transition-colors"
                                    ActiveClass="bg-white/10 font-semibold">
                                    Admin Dashboard
                                </NavLink>
                            }

                            <div class="border-t border-white/10 my-2"></div>

                            <!-- User info in mobile menu -->
                            <div class="px-3 py-2 flex items-center gap-3 justify-between">
                                <div class="flex items-center gap-3">
                                    @if (!string.IsNullOrEmpty(Auth.CurrentUser.ProfilePictureUrl))
                                    {
                                        <img src="@Auth.CurrentUser.ProfilePictureUrl" alt="@Auth.CurrentUser.FullName"
                                            class="h-10 w-10 rounded-full object-cover border-2 border-white/30" />
                                    }
                                    else
                                    {
                                        <div
                                            class="h-10 w-10 rounded-full @GetUserAvatarColor() flex items-center justify-center text-white text-sm font-semibold border-2 border-white/30">
                                            @GetUserInitials()
                                        </div>
                                    }
                                    <div class="flex-1">
                                        <div class="text-sm font-medium">@Auth.CurrentUser.FullName</div>
                                        <div class="text-xs text-gray-400">@Auth.CurrentUser.Email</div>
                                    </div>
                                </div>

                                <button type="button" @onclick="HandleSignOut"
                                    class="px-3 py-1.5 text-sm rounded-lg text-red-400 hover:bg-red-500/10 transition-colors whitespace-nowrap">
                                    Sign Out
                                </button>
                            </div>
                        }
                        else
                        {
                            <div class="border-t border-white/10 my-2"></div>
                            <div class="flex gap-2">
                                <a href="/signin" @onclick="CloseMobileMenu"
                                    class="flex-1 text-center px-3 py-2 rounded-lg hover:bg-white/10 transition-colors">
                                    Sign In
                                </a>
                                <a href="/signup" @onclick="CloseMobileMenu"
                                    class="flex-1 text-center px-3 py-2 rounded-lg bg-white text-black hover:bg-gray-100 transition-colors font-medium">
                                    Sign Up
                                </a>
                            </div>
                        }
                    </nav>
                </div>
            </div>
        </nav>
    </div>
</header>