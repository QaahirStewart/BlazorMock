<header class="py-6">
    <div class="max-w-6xl mx-auto px-4">
        <nav class="relative bg-black/85 text-white rounded-2xl px-4 md:py-6 py-4 shadow-xl">
            <div class="flex items-center justify-between gap-2">
                <button type="button" @onclick="ToggleMobileMenu"
                    class="md:hidden inline-flex items-center justify-center h-10 w-10 rounded-lg hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-white/40 order-1 transition-transform duration-200 @(isMobileMenuOpen ? "rotate-90" : string.Empty)"
                    aria-label="Toggle navigation" aria-expanded="@isMobileMenuOpen">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                        class="h-6 w-6">
                        @if (isMobileMenuOpen)
                        {
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M6 18L18 6M6 6l12 12" />
                        }
                        else
                        {
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M4 6h16M4 12h16M4 18h16" />
                        }
                    </svg>
                </button>

                <a href="/"
                    class="flex items-center gap-2 md:flex-initial flex-1 md:justify-start justify-center md:order-1 order-2">
                    <span
                        class="inline-flex h-8 w-8 items-center justify-center rounded-full bg-white/10 text-lg font-bold">A</span>
                    <span class="text-sm sm:text-base font-semibold tracking-wide wrap-break-word">Admin
                        Dashboard</span>
                </a>

                <ul
                    class="hidden md:flex items-center justify-center gap-4 lg:gap-6 text-sm text-gray-200 order-2 absolute left-1/2 -translate-x-1/2">
                    <li>
                        <NavLink href="/" Match="NavLinkMatch.All" class="hover:text-white transition-colors"
                            ActiveClass="text-white font-semibold underline underline-offset-4">Overview</NavLink>
                    </li>

                    @if (Auth.IsAuthenticated)
                    {
                        <li>
                            <NavLink href="/profile" class="hover:text-white transition-colors"
                                ActiveClass="text-white font-semibold underline underline-offset-4">Profile</NavLink>
                        </li>
                        <li>
                            <NavLink href="/analytics" class="hover:text-white transition-colors"
                                ActiveClass="text-white font-semibold underline underline-offset-4">Analytics</NavLink>
                        </li>

                        @if (Auth.CurrentUser?.Role == "Admin")
                        {
                            <li>
                                <NavLink href="/admin/dashboard" class="hover:text-white transition-colors"
                                    ActiveClass="text-white font-semibold underline underline-offset-4">Admin Dashboard
                                </NavLink>
                            </li>
                        }
                    }
                </ul>

                <div class="hidden md:block order-3">
                    @if (Auth.IsAuthenticated && Auth.CurrentUser != null)
                    {
                        <div class="relative">
                            <button type="button" @onclick="ToggleProfileMenu"
                                class="flex items-center gap-2 px-3 py-2 rounded-lg hover:bg-white/10 transition-colors">
                                @if (!string.IsNullOrEmpty(Auth.CurrentUser.ProfilePictureUrl))
                                {
                                    <img src="@Auth.CurrentUser.ProfilePictureUrl" alt="@Auth.CurrentUser.FullName"
                                        class="h-8 w-8 rounded-full object-cover border-2 border-white/30" />
                                }
                                else
                                {
                                    <div
                                        class="h-8 w-8 rounded-full @GetUserAvatarColor() flex items-center justify-center text-white text-xs font-semibold border-2 border-white/30">
                                        @GetUserInitials()
                                    </div>
                                }
                                <span class="text-sm font-medium">@Auth.CurrentUser.FullName</span>
                                <svg class="w-4 h-4 transition-transform @(isProfileMenuOpen ? "rotate-180" : string.Empty)"
                                    fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M19 9l-7 7-7-7" />
                                </svg>
                            </button>

                            @if (isProfileMenuOpen)
                            {
                                <div
                                    class="absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-xl border border-gray-200 py-1 z-50">
                                    <a href="/profile" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">My
                                        Profile</a>
                                    <a href="/analytics"
                                        class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">Analytics</a>
                                    @if (Auth.CurrentUser.Role == "Admin")
                                    {
                                        <a href="/admin/dashboard"
                                            class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">Admin Dashboard</a>
                                    }
                                    <div class="border-t border-gray-200 my-1"></div>
                                    <button type="button" @onclick="HandleSignOut"
                                        class="block w-full text-left px-4 py-2 text-sm text-red-600 hover:bg-red-50">Sign
                                        Out</button>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <div class="flex items-center gap-2">
                            <a href="/signin"
                                class="px-4 py-2 text-sm font-medium hover:bg-white/10 rounded-lg transition-colors">Sign
                                In</a>
                            <a href="/signup"
                                class="px-4 py-2 text-sm font-medium bg-white text-black rounded-lg hover:bg-gray-100 transition-colors">Sign
                                Up</a>
                        </div>
                    }
                </div>

                <div class="md:hidden w-10 order-3"></div>
            </div>

            <div
                class="md:hidden overflow-hidden transition-all duration-300 ease-in-out @(isMobileMenuOpen ? "max-h-96 opacity-100" : "max-h-0 opacity-0")">
                <div class="mt-3 border-t border-white/10 pt-3">
                    <nav class="flex flex-col gap-1">
                        <NavLink href="/" Match="NavLinkMatch.All"
                            class="px-3 py-2 text-sm sm:text-base rounded-lg hover:bg-white/10 transition-colors"
                            ActiveClass="bg-white/10 text-white font-semibold" @onclick="CloseMobileMenu">Overview
                        </NavLink>

                        @if (Auth.IsAuthenticated)
                        {
                            <NavLink href="/profile"
                                class="px-3 py-2 text-sm sm:text-base rounded-lg hover:bg-white/10 transition-colors"
                                ActiveClass="bg-white/10 text-white font-semibold" @onclick="CloseMobileMenu">Profile
                            </NavLink>
                            <NavLink href="/analytics"
                                class="px-3 py-2 text-sm sm:text-base rounded-lg hover:bg-white/10 transition-colors"
                                ActiveClass="bg-white/10 text-white font-semibold" @onclick="CloseMobileMenu">Analytics
                            </NavLink>

                            @if (Auth.CurrentUser?.Role == "Admin")
                            {
                                <NavLink href="/admin/dashboard"
                                    class="px-3 py-2 text-sm sm:text-base rounded-lg hover:bg-white/10 transition-colors"
                                    ActiveClass="bg-white/10 text-white font-semibold" @onclick="CloseMobileMenu">Admin
                                    Dashboard</NavLink>
                            }

                            <div class="border-t border-white/10 my-2"></div>
                            <button type="button" @onclick="HandleSignOut"
                                class="w-full text-left px-3 py-2 text-sm sm:text-base rounded-lg hover:bg-white/10 transition-colors text-red-400">Sign
                                Out</button>
                        }
                        else
                        {
                            <div class="border-t border-white/10 my-2"></div>
                            <NavLink href="/signin"
                                class="px-3 py-2 text-sm sm:text-base rounded-lg hover:bg-white/10 transition-colors"
                                ActiveClass="bg-white/10 text-white font-semibold" @onclick="CloseMobileMenu">Sign In
                            </NavLink>
                            <NavLink href="/signup"
                                class="px-3 py-2 text-sm sm:text-base rounded-lg bg-white/10 hover:bg-white/20 transition-colors"
                                ActiveClass="bg-white/20 text-white font-semibold" @onclick="CloseMobileMenu">Sign Up
                            </NavLink>
                        }
                    </nav>
                </div>
            </div>
        </nav>
    </div>
</header>